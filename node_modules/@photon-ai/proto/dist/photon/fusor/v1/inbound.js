import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
const protobufPackage = "photon.fusor.v1";
function createBaseRawInboundEvent() {
  return {
    eventId: "",
    projectId: "",
    receivedAt: void 0,
    sourceId: "",
    prevSubjectSeq: 0,
    platform: "",
    rawRequest: new Uint8Array(0)
  };
}
const RawInboundEvent = {
  encode(message, writer = new BinaryWriter()) {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.projectId !== "") {
      writer.uint32(18).string(message.projectId);
    }
    if (message.receivedAt !== void 0) {
      Timestamp.encode(toTimestamp(message.receivedAt), writer.uint32(34).fork()).join();
    }
    if (message.sourceId !== "") {
      writer.uint32(42).string(message.sourceId);
    }
    if (message.prevSubjectSeq !== 0) {
      writer.uint32(120).uint64(message.prevSubjectSeq);
    }
    if (message.platform !== "") {
      writer.uint32(130).string(message.platform);
    }
    if (message.rawRequest.length !== 0) {
      writer.uint32(138).bytes(message.rawRequest);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRawInboundEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.projectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.receivedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.sourceId = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }
          message.prevSubjectSeq = longToNumber(reader.uint64());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }
          message.platform = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }
          message.rawRequest = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : isSet(object.event_id) ? globalThis.String(object.event_id) : "",
      projectId: isSet(object.projectId) ? globalThis.String(object.projectId) : isSet(object.project_id) ? globalThis.String(object.project_id) : "",
      receivedAt: isSet(object.receivedAt) ? fromJsonTimestamp(object.receivedAt) : isSet(object.received_at) ? fromJsonTimestamp(object.received_at) : void 0,
      sourceId: isSet(object.sourceId) ? globalThis.String(object.sourceId) : isSet(object.source_id) ? globalThis.String(object.source_id) : "",
      prevSubjectSeq: isSet(object.prevSubjectSeq) ? globalThis.Number(object.prevSubjectSeq) : isSet(object.prev_subject_seq) ? globalThis.Number(object.prev_subject_seq) : 0,
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      rawRequest: isSet(object.rawRequest) ? bytesFromBase64(object.rawRequest) : isSet(object.raw_request) ? bytesFromBase64(object.raw_request) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.receivedAt !== void 0) {
      obj.receivedAt = message.receivedAt.toISOString();
    }
    if (message.sourceId !== "") {
      obj.sourceId = message.sourceId;
    }
    if (message.prevSubjectSeq !== 0) {
      obj.prevSubjectSeq = Math.round(message.prevSubjectSeq);
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.rawRequest.length !== 0) {
      obj.rawRequest = base64FromBytes(message.rawRequest);
    }
    return obj;
  },
  create(base) {
    return RawInboundEvent.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRawInboundEvent();
    message.eventId = object.eventId ?? "";
    message.projectId = object.projectId ?? "";
    message.receivedAt = object.receivedAt ?? void 0;
    message.sourceId = object.sourceId ?? "";
    message.prevSubjectSeq = object.prevSubjectSeq ?? 0;
    message.platform = object.platform ?? "";
    message.rawRequest = object.rawRequest ?? new Uint8Array(0);
    return message;
  }
};
function createBaseInboundReply() {
  return { eventId: "", errorReason: "", status: 0, headers: {}, body: new Uint8Array(0) };
}
const InboundReply = {
  encode(message, writer = new BinaryWriter()) {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.errorReason !== "") {
      writer.uint32(18).string(message.errorReason);
    }
    if (message.status !== 0) {
      writer.uint32(24).uint32(message.status);
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]) => {
      InboundReply_HeadersEntry.encode({ key, value }, writer.uint32(34).fork()).join();
    });
    if (message.body.length !== 0) {
      writer.uint32(42).bytes(message.body);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInboundReply();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.errorReason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.status = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          const entry4 = InboundReply_HeadersEntry.decode(reader, reader.uint32());
          if (entry4.value !== void 0) {
            message.headers[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.body = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      eventId: isSet(object.eventId) ? globalThis.String(object.eventId) : isSet(object.event_id) ? globalThis.String(object.event_id) : "",
      errorReason: isSet(object.errorReason) ? globalThis.String(object.errorReason) : isSet(object.error_reason) ? globalThis.String(object.error_reason) : "",
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      headers: isObject(object.headers) ? globalThis.Object.entries(object.headers).reduce(
        (acc, [key, value]) => {
          acc[key] = globalThis.String(value);
          return acc;
        },
        {}
      ) : {},
      body: isSet(object.body) ? bytesFromBase64(object.body) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.errorReason !== "") {
      obj.errorReason = message.errorReason;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers);
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    return obj;
  },
  create(base) {
    return InboundReply.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInboundReply();
    message.eventId = object.eventId ?? "";
    message.errorReason = object.errorReason ?? "";
    message.status = object.status ?? 0;
    message.headers = globalThis.Object.entries(object.headers ?? {}).reduce(
      (acc, [key, value]) => {
        if (value !== void 0) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {}
    );
    message.body = object.body ?? new Uint8Array(0);
    return message;
  }
};
function createBaseInboundReply_HeadersEntry() {
  return { key: "", value: "" };
}
const InboundReply_HeadersEntry = {
  encode(message, writer = new BinaryWriter()) {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInboundReply_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },
  create(base) {
    return InboundReply_HeadersEntry.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInboundReply_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  }
};
function bytesFromBase64(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function toTimestamp(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function isSet(value) {
  return value !== null && value !== void 0;
}
export {
  InboundReply,
  InboundReply_HeadersEntry,
  RawInboundEvent,
  protobufPackage
};
