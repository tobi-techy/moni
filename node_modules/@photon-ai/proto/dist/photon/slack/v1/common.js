import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp.js";
const protobufPackage = "photon.slack.v1";
function createBaseStreamCursor() {
  return { opaque: "" };
}
const StreamCursor = {
  encode(message, writer = new BinaryWriter()) {
    if (message.opaque !== "") {
      writer.uint32(10).string(message.opaque);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseStreamCursor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.opaque = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { opaque: isSet(object.opaque) ? globalThis.String(object.opaque) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.opaque !== "") {
      obj.opaque = message.opaque;
    }
    return obj;
  },
  create(base) {
    return StreamCursor.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseStreamCursor();
    message.opaque = object.opaque ?? "";
    return message;
  }
};
function createBaseHeartbeat() {
  return { at: void 0 };
}
const Heartbeat = {
  encode(message, writer = new BinaryWriter()) {
    if (message.at !== void 0) {
      Timestamp.encode(toTimestamp(message.at), writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHeartbeat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { at: isSet(object.at) ? fromJsonTimestamp(object.at) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.at !== void 0) {
      obj.at = message.at.toISOString();
    }
    return obj;
  },
  create(base) {
    return Heartbeat.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseHeartbeat();
    message.at = object.at ?? void 0;
    return message;
  }
};
function createBaseInboundMessage() {
  return {
    channel: "",
    user: "",
    text: "",
    ts: "",
    threadTs: void 0,
    subtype: void 0,
    isFromMe: false,
    files: []
  };
}
const InboundMessage = {
  encode(message, writer = new BinaryWriter()) {
    if (message.channel !== "") {
      writer.uint32(10).string(message.channel);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.ts !== "") {
      writer.uint32(34).string(message.ts);
    }
    if (message.threadTs !== void 0) {
      writer.uint32(42).string(message.threadTs);
    }
    if (message.subtype !== void 0) {
      writer.uint32(50).string(message.subtype);
    }
    if (message.isFromMe !== false) {
      writer.uint32(56).bool(message.isFromMe);
    }
    for (const v of message.files) {
      File.encode(v, writer.uint32(82).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInboundMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.channel = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.ts = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.threadTs = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.subtype = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.files.push(File.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      ts: isSet(object.ts) ? globalThis.String(object.ts) : "",
      threadTs: isSet(object.threadTs) ? globalThis.String(object.threadTs) : isSet(object.thread_ts) ? globalThis.String(object.thread_ts) : void 0,
      subtype: isSet(object.subtype) ? globalThis.String(object.subtype) : void 0,
      isFromMe: isSet(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e) => File.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.ts !== "") {
      obj.ts = message.ts;
    }
    if (message.threadTs !== void 0) {
      obj.threadTs = message.threadTs;
    }
    if (message.subtype !== void 0) {
      obj.subtype = message.subtype;
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.files?.length) {
      obj.files = message.files.map((e) => File.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return InboundMessage.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInboundMessage();
    message.channel = object.channel ?? "";
    message.user = object.user ?? "";
    message.text = object.text ?? "";
    message.ts = object.ts ?? "";
    message.threadTs = object.threadTs ?? void 0;
    message.subtype = object.subtype ?? void 0;
    message.isFromMe = object.isFromMe ?? false;
    message.files = object.files?.map((e) => File.fromPartial(e)) || [];
    return message;
  }
};
function createBaseReaction() {
  return { user: "", itemChannel: "", itemTs: "", name: "", removed: false, isFromMe: false };
}
const Reaction = {
  encode(message, writer = new BinaryWriter()) {
    if (message.user !== "") {
      writer.uint32(10).string(message.user);
    }
    if (message.itemChannel !== "") {
      writer.uint32(18).string(message.itemChannel);
    }
    if (message.itemTs !== "") {
      writer.uint32(26).string(message.itemTs);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    if (message.removed !== false) {
      writer.uint32(40).bool(message.removed);
    }
    if (message.isFromMe !== false) {
      writer.uint32(48).bool(message.isFromMe);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseReaction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.user = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.itemChannel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.itemTs = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.removed = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      itemChannel: isSet(object.itemChannel) ? globalThis.String(object.itemChannel) : isSet(object.item_channel) ? globalThis.String(object.item_channel) : "",
      itemTs: isSet(object.itemTs) ? globalThis.String(object.itemTs) : isSet(object.item_ts) ? globalThis.String(object.item_ts) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      removed: isSet(object.removed) ? globalThis.Boolean(object.removed) : false,
      isFromMe: isSet(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.itemChannel !== "") {
      obj.itemChannel = message.itemChannel;
    }
    if (message.itemTs !== "") {
      obj.itemTs = message.itemTs;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.removed !== false) {
      obj.removed = message.removed;
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    return obj;
  },
  create(base) {
    return Reaction.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseReaction();
    message.user = object.user ?? "";
    message.itemChannel = object.itemChannel ?? "";
    message.itemTs = object.itemTs ?? "";
    message.name = object.name ?? "";
    message.removed = object.removed ?? false;
    message.isFromMe = object.isFromMe ?? false;
    return message;
  }
};
function createBaseInteractiveCallback() {
  return { type: "", user: "", isFromMe: false, rawPayloadJson: "" };
}
const InteractiveCallback = {
  encode(message, writer = new BinaryWriter()) {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.isFromMe !== false) {
      writer.uint32(24).bool(message.isFromMe);
    }
    if (message.rawPayloadJson !== "") {
      writer.uint32(82).string(message.rawPayloadJson);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseInteractiveCallback();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.rawPayloadJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      isFromMe: isSet(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false,
      rawPayloadJson: isSet(object.rawPayloadJson) ? globalThis.String(object.rawPayloadJson) : isSet(object.raw_payload_json) ? globalThis.String(object.raw_payload_json) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.rawPayloadJson !== "") {
      obj.rawPayloadJson = message.rawPayloadJson;
    }
    return obj;
  },
  create(base) {
    return InteractiveCallback.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseInteractiveCallback();
    message.type = object.type ?? "";
    message.user = object.user ?? "";
    message.isFromMe = object.isFromMe ?? false;
    message.rawPayloadJson = object.rawPayloadJson ?? "";
    return message;
  }
};
function createBaseSlashCommand() {
  return { command: "", text: "", user: "", channel: "", responseUrl: "", triggerId: "", isFromMe: false };
}
const SlashCommand = {
  encode(message, writer = new BinaryWriter()) {
    if (message.command !== "") {
      writer.uint32(10).string(message.command);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.user !== "") {
      writer.uint32(26).string(message.user);
    }
    if (message.channel !== "") {
      writer.uint32(34).string(message.channel);
    }
    if (message.responseUrl !== "") {
      writer.uint32(42).string(message.responseUrl);
    }
    if (message.triggerId !== "") {
      writer.uint32(50).string(message.triggerId);
    }
    if (message.isFromMe !== false) {
      writer.uint32(56).bool(message.isFromMe);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSlashCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.command = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.user = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.channel = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.responseUrl = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.triggerId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      responseUrl: isSet(object.responseUrl) ? globalThis.String(object.responseUrl) : isSet(object.response_url) ? globalThis.String(object.response_url) : "",
      triggerId: isSet(object.triggerId) ? globalThis.String(object.triggerId) : isSet(object.trigger_id) ? globalThis.String(object.trigger_id) : "",
      isFromMe: isSet(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.responseUrl !== "") {
      obj.responseUrl = message.responseUrl;
    }
    if (message.triggerId !== "") {
      obj.triggerId = message.triggerId;
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    return obj;
  },
  create(base) {
    return SlashCommand.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSlashCommand();
    message.command = object.command ?? "";
    message.text = object.text ?? "";
    message.user = object.user ?? "";
    message.channel = object.channel ?? "";
    message.responseUrl = object.responseUrl ?? "";
    message.triggerId = object.triggerId ?? "";
    message.isFromMe = object.isFromMe ?? false;
    return message;
  }
};
function createBaseAppMention() {
  return { channel: "", user: "", text: "", ts: "", isFromMe: false };
}
const AppMention = {
  encode(message, writer = new BinaryWriter()) {
    if (message.channel !== "") {
      writer.uint32(10).string(message.channel);
    }
    if (message.user !== "") {
      writer.uint32(18).string(message.user);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.ts !== "") {
      writer.uint32(34).string(message.ts);
    }
    if (message.isFromMe !== false) {
      writer.uint32(40).bool(message.isFromMe);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAppMention();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.channel = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.user = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.ts = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      ts: isSet(object.ts) ? globalThis.String(object.ts) : "",
      isFromMe: isSet(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.ts !== "") {
      obj.ts = message.ts;
    }
    if (message.isFromMe !== false) {
      obj.isFromMe = message.isFromMe;
    }
    return obj;
  },
  create(base) {
    return AppMention.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAppMention();
    message.channel = object.channel ?? "";
    message.user = object.user ?? "";
    message.text = object.text ?? "";
    message.ts = object.ts ?? "";
    message.isFromMe = object.isFromMe ?? false;
    return message;
  }
};
function createBaseFile() {
  return { id: "", name: "", mimetype: "", size: 0, urlPrivate: "" };
}
const File = {
  encode(message, writer = new BinaryWriter()) {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.mimetype !== "") {
      writer.uint32(26).string(message.mimetype);
    }
    if (message.size !== 0) {
      writer.uint32(32).int64(message.size);
    }
    if (message.urlPrivate !== "") {
      writer.uint32(42).string(message.urlPrivate);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.mimetype = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.size = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.urlPrivate = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      mimetype: isSet(object.mimetype) ? globalThis.String(object.mimetype) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      urlPrivate: isSet(object.urlPrivate) ? globalThis.String(object.urlPrivate) : isSet(object.url_private) ? globalThis.String(object.url_private) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.mimetype !== "") {
      obj.mimetype = message.mimetype;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.urlPrivate !== "") {
      obj.urlPrivate = message.urlPrivate;
    }
    return obj;
  },
  create(base) {
    return File.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFile();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.mimetype = object.mimetype ?? "";
    message.size = object.size ?? 0;
    message.urlPrivate = object.urlPrivate ?? "";
    return message;
  }
};
function toTimestamp(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet(value) {
  return value !== null && value !== void 0;
}
export {
  AppMention,
  File,
  Heartbeat,
  InboundMessage,
  InteractiveCallback,
  Reaction,
  SlashCommand,
  StreamCursor,
  protobufPackage
};
