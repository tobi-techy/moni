import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { File } from "./common.js";
const protobufPackage = "photon.slack.v1";
function createBaseUploadRequest() {
  return {
    channel: "",
    filename: "",
    mimetype: "",
    content: new Uint8Array(0),
    initialComment: void 0,
    threadTs: void 0
  };
}
const UploadRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.channel !== "") {
      writer.uint32(10).string(message.channel);
    }
    if (message.filename !== "") {
      writer.uint32(18).string(message.filename);
    }
    if (message.mimetype !== "") {
      writer.uint32(26).string(message.mimetype);
    }
    if (message.content.length !== 0) {
      writer.uint32(34).bytes(message.content);
    }
    if (message.initialComment !== void 0) {
      writer.uint32(42).string(message.initialComment);
    }
    if (message.threadTs !== void 0) {
      writer.uint32(50).string(message.threadTs);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.channel = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.filename = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.mimetype = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.content = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.initialComment = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.threadTs = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      mimetype: isSet(object.mimetype) ? globalThis.String(object.mimetype) : "",
      content: isSet(object.content) ? bytesFromBase64(object.content) : new Uint8Array(0),
      initialComment: isSet(object.initialComment) ? globalThis.String(object.initialComment) : isSet(object.initial_comment) ? globalThis.String(object.initial_comment) : void 0,
      threadTs: isSet(object.threadTs) ? globalThis.String(object.threadTs) : isSet(object.thread_ts) ? globalThis.String(object.thread_ts) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.mimetype !== "") {
      obj.mimetype = message.mimetype;
    }
    if (message.content.length !== 0) {
      obj.content = base64FromBytes(message.content);
    }
    if (message.initialComment !== void 0) {
      obj.initialComment = message.initialComment;
    }
    if (message.threadTs !== void 0) {
      obj.threadTs = message.threadTs;
    }
    return obj;
  },
  create(base) {
    return UploadRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadRequest();
    message.channel = object.channel ?? "";
    message.filename = object.filename ?? "";
    message.mimetype = object.mimetype ?? "";
    message.content = object.content ?? new Uint8Array(0);
    message.initialComment = object.initialComment ?? void 0;
    message.threadTs = object.threadTs ?? void 0;
    return message;
  }
};
function createBaseFileShare() {
  return { channel: "", ts: "" };
}
const FileShare = {
  encode(message, writer = new BinaryWriter()) {
    if (message.channel !== "") {
      writer.uint32(10).string(message.channel);
    }
    if (message.ts !== "") {
      writer.uint32(18).string(message.ts);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseFileShare();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.channel = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.ts = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      channel: isSet(object.channel) ? globalThis.String(object.channel) : "",
      ts: isSet(object.ts) ? globalThis.String(object.ts) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.channel !== "") {
      obj.channel = message.channel;
    }
    if (message.ts !== "") {
      obj.ts = message.ts;
    }
    return obj;
  },
  create(base) {
    return FileShare.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseFileShare();
    message.channel = object.channel ?? "";
    message.ts = object.ts ?? "";
    return message;
  }
};
function createBaseUploadResponse() {
  return { file: void 0, shares: [] };
}
const UploadResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.file !== void 0) {
      File.encode(message.file, writer.uint32(10).fork()).join();
    }
    for (const v of message.shares) {
      FileShare.encode(v, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.file = File.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.shares.push(FileShare.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      file: isSet(object.file) ? File.fromJSON(object.file) : void 0,
      shares: globalThis.Array.isArray(object?.shares) ? object.shares.map((e) => FileShare.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.file !== void 0) {
      obj.file = File.toJSON(message.file);
    }
    if (message.shares?.length) {
      obj.shares = message.shares.map((e) => FileShare.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return UploadResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadResponse();
    message.file = object.file !== void 0 && object.file !== null ? File.fromPartial(object.file) : void 0;
    message.shares = object.shares?.map((e) => FileShare.fromPartial(e)) || [];
    return message;
  }
};
function createBaseGetUrlRequest() {
  return { fileId: "" };
}
const GetUrlRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.fileId !== "") {
      writer.uint32(10).string(message.fileId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.fileId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : isSet(object.file_id) ? globalThis.String(object.file_id) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.fileId !== "") {
      obj.fileId = message.fileId;
    }
    return obj;
  },
  create(base) {
    return GetUrlRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetUrlRequest();
    message.fileId = object.fileId ?? "";
    return message;
  }
};
function createBaseGetUrlResponse() {
  return { url: "" };
}
const GetUrlResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetUrlResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { url: isSet(object.url) ? globalThis.String(object.url) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },
  create(base) {
    return GetUrlResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetUrlResponse();
    message.url = object.url ?? "";
    return message;
  }
};
function createBaseGetContentRequest() {
  return { fileId: "", offset: void 0 };
}
const GetContentRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.fileId !== "") {
      writer.uint32(10).string(message.fileId);
    }
    if (message.offset !== void 0) {
      writer.uint32(16).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetContentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.fileId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.offset = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      fileId: isSet(object.fileId) ? globalThis.String(object.fileId) : isSet(object.file_id) ? globalThis.String(object.file_id) : "",
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.fileId !== "") {
      obj.fileId = message.fileId;
    }
    if (message.offset !== void 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return GetContentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetContentRequest();
    message.fileId = object.fileId ?? "";
    message.offset = object.offset ?? void 0;
    return message;
  }
};
function createBaseGetContentHeader() {
  return { mimetype: "", name: "", size: 0, offset: 0 };
}
const GetContentHeader = {
  encode(message, writer = new BinaryWriter()) {
    if (message.mimetype !== "") {
      writer.uint32(10).string(message.mimetype);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.size !== 0) {
      writer.uint32(24).int64(message.size);
    }
    if (message.offset !== 0) {
      writer.uint32(32).int64(message.offset);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetContentHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.mimetype = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.size = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.offset = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      mimetype: isSet(object.mimetype) ? globalThis.String(object.mimetype) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.mimetype !== "") {
      obj.mimetype = message.mimetype;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },
  create(base) {
    return GetContentHeader.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetContentHeader();
    message.mimetype = object.mimetype ?? "";
    message.name = object.name ?? "";
    message.size = object.size ?? 0;
    message.offset = object.offset ?? 0;
    return message;
  }
};
function createBaseGetContentResponse() {
  return { header: void 0, chunk: void 0 };
}
const GetContentResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.header !== void 0) {
      GetContentHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.chunk !== void 0) {
      writer.uint32(18).bytes(message.chunk);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetContentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.header = GetContentHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.chunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      header: isSet(object.header) ? GetContentHeader.fromJSON(object.header) : void 0,
      chunk: isSet(object.chunk) ? bytesFromBase64(object.chunk) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.header !== void 0) {
      obj.header = GetContentHeader.toJSON(message.header);
    }
    if (message.chunk !== void 0) {
      obj.chunk = base64FromBytes(message.chunk);
    }
    return obj;
  },
  create(base) {
    return GetContentResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetContentResponse();
    message.header = object.header !== void 0 && object.header !== null ? GetContentHeader.fromPartial(object.header) : void 0;
    message.chunk = object.chunk ?? void 0;
    return message;
  }
};
const FileServiceDefinition = {
  name: "FileService",
  fullName: "photon.slack.v1.FileService",
  methods: {
    upload: {
      name: "Upload",
      requestType: UploadRequest,
      requestStream: false,
      responseType: UploadResponse,
      responseStream: false,
      options: {}
    },
    getUrl: {
      name: "GetUrl",
      requestType: GetUrlRequest,
      requestStream: false,
      responseType: GetUrlResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Server-streams the file's bytes through spectrum-slack so SDKs (which
     * never hold the bot token) can consume the content. First frame is a
     * header (mimetype/name/size/offset); subsequent frames are byte chunks.
     * Supports byte-range resume via `GetContentRequest.offset` so an SDK can
     * recover from a mid-stream disconnect (pod redeploy, network blip)
     * without re-downloading bytes it already has.
     */
    getContent: {
      name: "GetContent",
      requestType: GetContentRequest,
      requestStream: false,
      responseType: GetContentResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase64(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet(value) {
  return value !== null && value !== void 0;
}
export {
  FileServiceDefinition,
  FileShare,
  GetContentHeader,
  GetContentRequest,
  GetContentResponse,
  GetUrlRequest,
  GetUrlResponse,
  UploadRequest,
  UploadResponse,
  protobufPackage
};
