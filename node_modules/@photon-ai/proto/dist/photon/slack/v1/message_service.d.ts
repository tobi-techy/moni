import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';
import { CallOptions, CallContext } from 'nice-grpc-common';

declare const protobufPackage = "photon.slack.v1";
/**
 * MessageService — the Slack runtime contract for spectrum-ts.
 *
 * See slack.md "Runtime contract (gRPC)" for the full design. Every RPC
 * requires two metadata keys: `access_token` (LightAuth JWT) and `team_id`.
 */
interface SendMessageRequest {
    channel: string;
    text?: TextContent | undefined;
    blocks?: BlocksContent | undefined;
    attachments?: AttachmentsContent | undefined;
    reaction?: ReactionContent | undefined;
    threadTs?: string | undefined;
    replyBroadcast?: boolean | undefined;
}
declare const SendMessageRequest: MessageFns<SendMessageRequest>;
interface TextContent {
    body: string;
}
declare const TextContent: MessageFns<TextContent>;
interface BlocksContent {
    /**
     * Slack's Block Kit, serialized as JSON to keep us provider-faithful without
     * having to keep a parallel proto schema in lockstep with Slack changes.
     */
    blocksJson: string;
    /** Optional fallback text shown in notifications / clients that don't render blocks. */
    fallbackText: string;
}
declare const BlocksContent: MessageFns<BlocksContent>;
interface AttachmentsContent {
    attachmentsJson: string;
    text: string;
}
declare const AttachmentsContent: MessageFns<AttachmentsContent>;
interface ReactionContent {
    itemTs: string;
    itemChannel: string;
    emoji: string;
}
declare const ReactionContent: MessageFns<ReactionContent>;
interface SendMessageResponse {
    ts: string;
    channel: string;
}
declare const SendMessageResponse: MessageFns<SendMessageResponse>;
interface MarkReadRequest {
    channel: string;
    ts: string;
}
declare const MarkReadRequest: MessageFns<MarkReadRequest>;
interface MarkReadResponse {
}
declare const MarkReadResponse: MessageFns<MarkReadResponse>;
interface WhoAmIRequest {
}
declare const WhoAmIRequest: MessageFns<WhoAmIRequest>;
interface WhoAmIResponse {
    /** The bot user id (e.g. "U0123456789") for this installation. */
    botUserId: string;
    /** Echoes the team_id metadata the call was authorized against. */
    teamId: string;
    /** Slack's api_app_id for the installed app. */
    appId: string;
}
declare const WhoAmIResponse: MessageFns<WhoAmIResponse>;
type MessageServiceDefinition = typeof MessageServiceDefinition;
declare const MessageServiceDefinition: {
    readonly name: "MessageService";
    readonly fullName: "photon.slack.v1.MessageService";
    readonly methods: {
        /**
         * Outbound (unary). Inbound delivery now lives on the cross-channel
         * photon.fusor.internal.v1.EventsService served by spectrum-grpc-fanout — SDK
         * clients connect there for `Subscribe` instead of calling
         * `SubscribeEvents` / `FetchMissedEvents` here. Slack ingress
         * (POST /slack/events) still lands on this service's host, but the
         * verified event hops onto NATS rather than fanning out via gRPC streams.
         */
        readonly sendMessage: {
            readonly name: "SendMessage";
            readonly requestType: typeof SendMessageRequest;
            readonly requestStream: false;
            readonly responseType: typeof SendMessageResponse;
            readonly responseStream: false;
            readonly options: {};
        };
        readonly markRead: {
            readonly name: "MarkRead";
            readonly requestType: typeof MarkReadRequest;
            readonly requestStream: false;
            readonly responseType: typeof MarkReadResponse;
            readonly responseStream: false;
            readonly options: {};
        };
        /**
         * Identity. Returns the installed bot's identity for the authenticated
         * (project, team). Resolved from the server-side installation cache;
         * never round-trips Slack.
         */
        readonly whoAmI: {
            readonly name: "WhoAmI";
            readonly requestType: typeof WhoAmIRequest;
            readonly requestStream: false;
            readonly responseType: typeof WhoAmIResponse;
            readonly responseStream: false;
            readonly options: {};
        };
    };
};
interface MessageServiceImplementation<CallContextExt = {}> {
    /**
     * Outbound (unary). Inbound delivery now lives on the cross-channel
     * photon.fusor.internal.v1.EventsService served by spectrum-grpc-fanout — SDK
     * clients connect there for `Subscribe` instead of calling
     * `SubscribeEvents` / `FetchMissedEvents` here. Slack ingress
     * (POST /slack/events) still lands on this service's host, but the
     * verified event hops onto NATS rather than fanning out via gRPC streams.
     */
    sendMessage(request: SendMessageRequest, context: CallContext & CallContextExt): Promise<DeepPartial<SendMessageResponse>>;
    markRead(request: MarkReadRequest, context: CallContext & CallContextExt): Promise<DeepPartial<MarkReadResponse>>;
    /**
     * Identity. Returns the installed bot's identity for the authenticated
     * (project, team). Resolved from the server-side installation cache;
     * never round-trips Slack.
     */
    whoAmI(request: WhoAmIRequest, context: CallContext & CallContextExt): Promise<DeepPartial<WhoAmIResponse>>;
}
interface MessageServiceClient<CallOptionsExt = {}> {
    /**
     * Outbound (unary). Inbound delivery now lives on the cross-channel
     * photon.fusor.internal.v1.EventsService served by spectrum-grpc-fanout — SDK
     * clients connect there for `Subscribe` instead of calling
     * `SubscribeEvents` / `FetchMissedEvents` here. Slack ingress
     * (POST /slack/events) still lands on this service's host, but the
     * verified event hops onto NATS rather than fanning out via gRPC streams.
     */
    sendMessage(request: DeepPartial<SendMessageRequest>, options?: CallOptions & CallOptionsExt): Promise<SendMessageResponse>;
    markRead(request: DeepPartial<MarkReadRequest>, options?: CallOptions & CallOptionsExt): Promise<MarkReadResponse>;
    /**
     * Identity. Returns the installed bot's identity for the authenticated
     * (project, team). Resolved from the server-side installation cache;
     * never round-trips Slack.
     */
    whoAmI(request: DeepPartial<WhoAmIRequest>, options?: CallOptions & CallOptionsExt): Promise<WhoAmIResponse>;
}
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
type DeepPartial<T> = T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {} ? {
    [K in keyof T]?: DeepPartial<T[K]>;
} : Partial<T>;
interface MessageFns<T> {
    encode(message: T, writer?: BinaryWriter): BinaryWriter;
    decode(input: BinaryReader | Uint8Array, length?: number): T;
    fromJSON(object: any): T;
    toJSON(message: T): unknown;
    create(base?: DeepPartial<T>): T;
    fromPartial(object: DeepPartial<T>): T;
}

export { AttachmentsContent, BlocksContent, type DeepPartial, MarkReadRequest, MarkReadResponse, type MessageFns, type MessageServiceClient, MessageServiceDefinition, type MessageServiceImplementation, ReactionContent, SendMessageRequest, SendMessageResponse, TextContent, WhoAmIRequest, WhoAmIResponse, protobufPackage };
