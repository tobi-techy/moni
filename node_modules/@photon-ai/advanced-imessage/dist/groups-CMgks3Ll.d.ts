/**
 * Canonical error codes returned by the server.
 *
 * Modelled as an `as const` object so that both the runtime values and the
 * union type are available, with full autocomplete.
 */
declare const ErrorCode: {
    readonly unauthenticated: "unauthenticated";
    readonly tokenExpired: "tokenExpired";
    readonly tokenBlocked: "tokenBlocked";
    readonly unauthorized: "unauthorized";
    readonly dailyLimitExceeded: "dailyLimitExceeded";
    readonly recipientLimitExceeded: "recipientLimitExceeded";
    readonly uploadRateExceeded: "uploadRateExceeded";
    readonly contentDuplicateExceeded: "contentDuplicateExceeded";
    readonly recipientCoolingDown: "recipientCoolingDown";
    readonly recipientLocked: "recipientLocked";
    readonly sendReceiveRatioExceeded: "sendReceiveRatioExceeded";
    readonly duplicateMessage: "duplicateMessage";
    readonly chatNotFound: "chatNotFound";
    readonly messageNotFound: "messageNotFound";
    readonly attachmentNotFound: "attachmentNotFound";
    readonly addressNotFound: "addressNotFound";
    readonly sharedFriendLocationNotFound: "sharedFriendLocationNotFound";
    readonly groupIconNotFound: "groupIconNotFound";
    readonly pollNotFound: "pollNotFound";
    readonly invalidArgument: "invalidArgument";
    readonly preconditionFailed: "preconditionFailed";
    readonly operationNotSupported: "operationNotSupported";
    readonly attachmentNotReady: "attachmentNotReady";
    readonly privateApiUnavailable: "privateApiUnavailable";
    readonly serviceUnavailable: "serviceUnavailable";
    readonly timeout: "timeout";
    readonly internalError: "internalError";
    readonly databaseError: "databaseError";
    readonly networkError: "networkError";
};
/** Union of all known error code strings. */
type ErrorCode = (typeof ErrorCode)[keyof typeof ErrorCode];

/**
 * Error class hierarchy for the advanced-imessage-ts SDK.
 *
 * Inspired by Stripe's error design: a single base class with typed subclasses
 * so callers can use `instanceof` to handle specific failure modes rather than
 * inspecting boolean flags or string codes.
 *
 * A factory function in `error-handler.ts` maps gRPC errors to the correct
 * subclass automatically.
 */

interface IMessageErrorOptions {
    /** The original error that caused this one. */
    readonly cause?: Error;
    /** Canonical error code from the server. */
    readonly code: ErrorCode;
    /** Arbitrary key-value pairs providing additional context. */
    readonly context?: Record<string, string>;
    /** Numeric gRPC status code (mirrors `nice-grpc-common` Status enum). */
    readonly grpcCode: number;
    /**
     * Correlation id assigned by the HTTP middleware (`x-request-id` header /
     * body `requestId`) — quote it when reporting issues; operators can look
     * up the request's logs and trace from it.
     */
    readonly requestId?: string;
    /** Server-requested retry delay in milliseconds (`Retry-After`). */
    readonly retryAfter?: number;
    /** Whether the caller should retry the request. */
    readonly retryable: boolean;
    /**
     * Where the error originated: `"upstream"` (a Mac backend),
     * `"spectrum-imessage"` (the proxy), `"middleware"` (the HTTP
     * transcoder), or `"intermediary"` (a load balancer/gateway that
     * answered without the middleware's error contract — the status code is
     * approximate and the error is infrastructure, not semantics).
     */
    readonly source?: string;
}
/**
 * Base error for every failure surfaced by the SDK.
 *
 * All properties are `readonly` -- errors are informational, not mutable.
 */
declare class IMessageError extends Error {
    readonly code: ErrorCode;
    readonly retryable: boolean;
    readonly grpcCode: number;
    readonly context: Record<string, string>;
    readonly retryAfter?: number;
    readonly source?: string;
    readonly requestId?: string;
    constructor(message: string, options: IMessageErrorOptions);
}
/**
 * The request could not be authenticated or the caller lacks permission.
 *
 * Maps from gRPC `UNAUTHENTICATED` and `PERMISSION_DENIED`.
 */
declare class AuthenticationError extends IMessageError {
    constructor(message: string, options: IMessageErrorOptions);
}
/**
 * The requested resource was not found.
 *
 * Maps from gRPC `NOT_FOUND`.
 */
declare class NotFoundError extends IMessageError {
    constructor(message: string, options: IMessageErrorOptions);
}
/**
 * A rate limit or quota was exceeded.
 *
 * Maps from gRPC `RESOURCE_EXHAUSTED`.
 */
declare class RateLimitError extends IMessageError {
    constructor(message: string, options: IMessageErrorOptions);
}
/**
 * The request contained invalid arguments or a failed precondition.
 *
 * Maps from gRPC `INVALID_ARGUMENT` and `FAILED_PRECONDITION`.
 */
declare class ValidationError extends IMessageError {
    constructor(message: string, options: IMessageErrorOptions);
}
/**
 * The server is unreachable or the request timed out.
 *
 * Maps from gRPC `UNAVAILABLE` and `DEADLINE_EXCEEDED`.
 */
declare class ConnectionError extends IMessageError {
    constructor(message: string, options: IMessageErrorOptions);
}

/**
 * Lightweight string-union enums for the SDK's public API.
 *
 * These replace the generated protobuf numeric enums with human-readable
 * string literals. The mapper layer handles conversion to/from wire values.
 */
/** Current transfer state of an attachment. */
type TransferState = "pending" | "transferring" | "failed" | "finished" | "unknown";
/** Discriminates the kind of item a message represents. */
type MessageItemType = "normal" | "groupNameChange" | "participantChange" | "chatAction" | "unknown";
/** The underlying transport service for a chat or address. */
type ChatServiceType = "iMessage" | "SMS" | "RCS" | "unknown";
/** Sidecar attachment kind. */
type CompanionKind = "live-photo-video" | "unknown";

/**
 * Address-related domain types.
 */

/**
 * An address bound to one concrete transport service.
 */
interface SingleServiceAddressInfo {
    /** Canonical email or E.164 phone number for this participant. */
    readonly address: string;
    /** ISO 3166-1 alpha-2 country code when Messages has one, else omitted. */
    readonly country?: string;
    /** The concrete service this participant record is bound to. */
    readonly service: ChatServiceType;
}
/** A canonical address paired with every service reported by the server. */
interface MultiServiceAddressInfo {
    /** Canonical email or E.164 phone number. */
    readonly address: string;
    /** ISO 3166-1 alpha-2 country code when known, else `null`. */
    readonly country: string | null;
    /** Service set reported by the server. */
    readonly services: readonly ChatServiceType[];
}

/**
 * Attachment-related domain types.
 */

interface AttachmentInfo {
    readonly companionKind?: CompanionKind;
    readonly fileName: string;
    readonly guid: string;
    readonly isHidden: boolean;
    readonly isOutgoing: boolean;
    readonly isSticker: boolean;
    readonly mimeType: string;
    readonly originalGuid?: string;
    readonly totalBytes: number;
    readonly transferState: TransferState;
    readonly uti: string;
}
interface CompanionInfo {
    readonly fileName: string;
    readonly kind: CompanionKind;
    readonly mimeType: string;
    readonly totalBytes: number;
}
interface UploadAttachmentResult {
    /** Primary attachment metadata. Use `attachment.guid` with `messages.sendAttachment(...)`. */
    readonly attachment: AttachmentInfo;
    /** Live Photo companion metadata when `input.companion` was provided. */
    readonly companion?: CompanionInfo;
}
interface AttachmentInput {
    /** Optional Live Photo video bytes paired with the primary image. */
    readonly companion?: {
        /** Raw companion video bytes. */
        readonly data: Uint8Array;
    };
    /** Raw file bytes to upload. */
    readonly data: Uint8Array;
    /** Display filename stored with the attachment, including extension. */
    readonly fileName: string;
}
type DownloadAttachmentChunk = {
    /** First frame: attachment metadata and optional companion metadata. */
    readonly type: "header";
    readonly info: AttachmentInfo;
    readonly companionInfo?: CompanionInfo;
} | {
    /** Byte chunk for the primary attachment. */
    readonly type: "primaryChunk";
    readonly data: Uint8Array;
} | {
    /** Byte chunk for the Live Photo companion video. */
    readonly type: "companionChunk";
    readonly data: Uint8Array;
};

/**
 * Message effects and text effects.
 *
 * Apple identifies effects by long reverse-DNS strings. These `as const`
 * objects provide friendly names with full autocomplete and type narrowing --
 * the developer never needs to type (or see) the raw identifiers.
 */
/**
 * Full-screen and bubble effects that can be applied when sending a message.
 *
 * @example
 * ```ts
 * await im.messages.sendText(chat, "Happy birthday!", {
 *   effect: MessageEffect.confetti,
 * });
 * ```
 */
declare const MessageEffect: {
    readonly slam: "com.apple.MobileSMS.expressivesend.impact";
    readonly loud: "com.apple.MobileSMS.expressivesend.loud";
    readonly gentle: "com.apple.MobileSMS.expressivesend.gentle";
    readonly invisible: "com.apple.MobileSMS.expressivesend.invisibleink";
    readonly confetti: "com.apple.messages.effect.CKConfettiEffect";
    readonly fireworks: "com.apple.messages.effect.CKFireworksEffect";
    readonly balloons: "com.apple.messages.effect.CKBalloonEffect";
    readonly heart: "com.apple.messages.effect.CKHeartEffect";
    readonly lasers: "com.apple.messages.effect.CKLasersEffect";
    readonly celebration: "com.apple.messages.effect.CKHappyBirthdayEffect";
    readonly sparkles: "com.apple.messages.effect.CKSparklesEffect";
    readonly spotlight: "com.apple.messages.effect.CKSpotlightEffect";
    readonly echo: "com.apple.messages.effect.CKEchoEffect";
};
/** Union of all valid message-effect ID strings. */
type MessageEffect = (typeof MessageEffect)[keyof typeof MessageEffect];
/**
 * Per-character text effects (iOS 18+) that animate individual words or
 * phrases within a message.
 */
declare const TextEffect: {
    readonly big: "big";
    readonly small: "small";
    readonly shake: "shake";
    readonly nod: "nod";
    readonly explode: "explode";
    readonly ripple: "ripple";
    readonly bloom: "bloom";
    readonly jitter: "jitter";
};
/** Union of all valid text-effect identifier strings. */
type TextEffect = (typeof TextEffect)[keyof typeof TextEffect];

/**
 * Message-related domain types.
 */

interface TextFormat {
    readonly effectName?: string;
    readonly length: number;
    readonly start: number;
    readonly type: string;
}
type TextFormatInput = {
    readonly type: "bold";
    readonly start: number;
    readonly length: number;
} | {
    readonly type: "italic";
    readonly start: number;
    readonly length: number;
} | {
    readonly type: "underline";
    readonly start: number;
    readonly length: number;
} | {
    readonly type: "strikethrough";
    readonly start: number;
    readonly length: number;
} | {
    readonly type: "effect";
    readonly start: number;
    readonly length: number;
    readonly effect: TextEffect;
};
interface MessageMention {
    readonly address: string;
    readonly length: number;
    readonly start: number;
}
interface MessageReaction {
    readonly emoji?: string;
    readonly kind: "love" | "like" | "dislike" | "laugh" | "emphasize" | "question" | "emoji" | "sticker" | "unknown";
}
interface SettableMessageReaction {
    /** Emoji character to send when `kind` is `"emoji"`. */
    readonly emoji?: string;
    /** Tapback family to add or remove. Use `placeSticker(...)` for stickers. */
    readonly kind: "love" | "like" | "dislike" | "laugh" | "emphasize" | "question" | "emoji";
}
interface StickerPlacement {
    /** Optional clockwise rotation in Apple's normalized coordinate space. */
    readonly rotation?: number;
    /** Optional scale factor for the sticker. */
    readonly scale?: number;
    /** Optional rendered sticker width. */
    readonly width?: number;
    /** Horizontal position in Apple's normalized coordinate space. */
    readonly x: number;
    /** Vertical position in Apple's normalized coordinate space. */
    readonly y: number;
}
/** Visible fields decoded from an inbound iMessage mini-app card. */
interface MiniAppLayoutInfo {
    /** Top-left, bold. The most prominent text slot. */
    readonly caption?: string;
    /** Overlay text shown below `imageTitle`, above the image edge. */
    readonly imageSubtitle?: string;
    /** Overlay text shown above the image. */
    readonly imageTitle?: string;
    /** Below `caption`, on the left. */
    readonly subcaption?: string;
    /** Fallback text shown when the full card cannot be rendered. */
    readonly summary?: string;
    /** Top-right. */
    readonly trailingCaption?: string;
    /** Below `trailingCaption`, on the right. */
    readonly trailingSubcaption?: string;
}
/** Public semantic data decoded from an inbound iMessage app-extension balloon. */
interface MiniAppContent {
    /** Display name embedded by the sending extension. */
    readonly appName?: string;
    /** App Store identifier supplied by the sender. */
    readonly appStoreId?: number;
    /** iMessage extension bundle identifier parsed from the balloon id. */
    readonly extensionBundleId: string;
    /** Visible template fields decoded from the payload. */
    readonly layout?: MiniAppLayoutInfo;
    /** Whether the payload carries live-layout metadata. */
    readonly live: boolean;
    /** Stable session identifier shared by updates to the same card. */
    readonly sessionId?: string;
    /** Apple Team ID parsed from the balloon id. */
    readonly teamId: string;
    /** URL delivered to the extension when the recipient opens the card. */
    readonly url?: string;
}
interface MessageContent {
    readonly attachments: readonly AttachmentInfo[];
    readonly balloonBundleId?: string;
    readonly expressiveSendStyleId?: string;
    readonly formatting: readonly TextFormat[];
    readonly mentions: readonly MessageMention[];
    readonly miniApp?: MiniAppContent;
    readonly text?: string;
}
interface MessageAppliedReaction {
    readonly dateCreated: Date;
    readonly isFromMe: boolean;
    readonly messageGuid: string;
    readonly reaction: MessageReaction;
    readonly sender?: SingleServiceAddressInfo;
    readonly targetPartIndex?: number;
}
interface MessagePlacedSticker {
    readonly dateCreated: Date;
    readonly isFromMe: boolean;
    readonly messageGuid: string;
    readonly placement?: StickerPlacement;
    readonly sender?: SingleServiceAddressInfo;
    readonly sticker?: AttachmentInfo;
    readonly targetPartIndex?: number;
}
interface Message {
    readonly appliedReactions: readonly MessageAppliedReaction[];
    readonly cachedRoomNames?: string;
    readonly chatActionType?: number;
    readonly chatGuids: readonly string[];
    readonly content: MessageContent;
    readonly dataDetectorResultsPresent: boolean;
    readonly dateCreated: Date;
    readonly dateDelivered?: Date;
    readonly dateEdited?: Date;
    readonly dateExpressiveSendPlayed?: Date;
    readonly datePlayed?: Date;
    readonly dateRead?: Date;
    readonly dateRetracted?: Date;
    readonly destinationCallerId?: string;
    readonly didNotifyRecipient: boolean;
    readonly groupTitle?: string;
    readonly guid: string;
    readonly isArchived: boolean;
    readonly isAudioMessage: boolean;
    readonly isAutoReply: boolean;
    readonly isCorrupt: boolean;
    readonly isDelayed: boolean;
    readonly isDelivered: boolean;
    readonly isDeliveredQuietly: boolean;
    readonly isExpirable: boolean;
    readonly isForward: boolean;
    readonly isFromMe: boolean;
    readonly isSent: boolean;
    readonly isServiceMessage: boolean;
    readonly isSpam: boolean;
    readonly isSystemMessage: boolean;
    readonly itemType: MessageItemType;
    readonly partCount?: number;
    readonly placedStickers: readonly MessagePlacedSticker[];
    readonly reaction?: MessageReaction;
    readonly reactionSelected?: boolean;
    readonly reactionTargetGuid?: string;
    readonly reactionTargetPartIndex?: number;
    readonly replyTargetGuid?: string;
    readonly sendErrorCode: number;
    readonly sender?: SingleServiceAddressInfo;
    readonly shareDirection?: number;
    readonly shareStatus?: number;
    readonly subject?: string;
    readonly threadOriginatorGuid?: string;
    readonly threadOriginatorPart?: string;
}
interface SendOptions {
    /** Stable idempotency key for this logical send. */
    readonly clientMessageId?: string;
    /** Full-screen or bubble effect to apply to the outgoing message. */
    readonly effect?: MessageEffect;
    /** Enable Apple's data-detector pass for links, dates, addresses, and similar text. */
    readonly enableDataDetection?: boolean;
    /** Ask Messages to generate rich URL previews when possible. */
    readonly enableLinkPreview?: boolean;
    /** UTF-16 ranges for bold, italic, underline, strikethrough, or text effects. */
    readonly formatting?: readonly TextFormatInput[];
    /** Message guid, or message guid plus multipart bubble index, to reply to. */
    readonly replyTo?: string | {
        readonly guid: string;
        readonly partIndex?: number;
    };
    /** Optional subject line for the outgoing message. */
    readonly subject?: string;
}
/**
 * Visible layout of an iMessage mini-app card. Mirrors Apple's
 * `MSMessageTemplateLayout`. At least one of `caption`, `subcaption`,
 * `trailingCaption`, `trailingSubcaption`, or `image` must be set;
 * an entirely empty layout renders as a blank bubble and is rejected.
 *
 * Slot map: `caption` / `subcaption` render on the left,
 * `trailingCaption` / `trailingSubcaption` render on the right, and
 * `imageTitle` / `imageSubtitle` overlay `image`.
 * `image` and `imageTitle` must be set together; `imageSubtitle`
 * requires `image`.
 */
interface MiniAppLayout {
    /** Top-left, bold. The most prominent text slot. */
    readonly caption?: string;
    /** JPEG preview image bytes. */
    readonly image?: Uint8Array;
    /** Overlay text shown below `imageTitle`. Requires `image`. */
    readonly imageSubtitle?: string;
    /** Overlay text shown above the image. Must be set together with `image`. */
    readonly imageTitle?: string;
    /** Below `caption`, on the left. */
    readonly subcaption?: string;
    /** Fallback text for surfaces that cannot render the full card. */
    readonly summary?: string;
    /** Top-right. */
    readonly trailingCaption?: string;
    /** Below `trailingCaption`, on the right. */
    readonly trailingSubcaption?: string;
}
interface CustomizedMiniAppMessage {
    /** Display name of the owning app, shown by Messages fallback UI. */
    readonly appName: string;
    /** Apple App Store numeric id of the owning app. When set, must be a positive integer. */
    readonly appStoreId?: number;
    /** Bundle identifier of the iMessage extension target. Must not contain `:`. */
    readonly extensionBundleId: string;
    /** Visible card layout. */
    readonly layout: MiniAppLayout;
    /** Render with the installed extension's live UI when available. Defaults to `false`. */
    readonly live?: boolean;
    /** 10-character uppercase alphanumeric Apple Team ID. */
    readonly teamId: string;
    /** Absolute HTTP or HTTPS URL delivered to the installed extension on tap. */
    readonly url: string;
}
/**
 * Stable mini-app card handle returned by mini-app send/update calls. Save it
 * and pass it unchanged to the matching update call.
 */
interface MiniAppCardSession {
    /** Chat GUID accepted by IMAgentKit for future updates. */
    readonly chatGuid: string;
    /** Message GUID returned by the card send/update primitive. */
    readonly messageGuid: string;
    /** Stable UUID reused across in-place updates. */
    readonly sessionId: string;
    /** Original card bubble GUID that future updates replace in place. */
    readonly targetMessageGuid: string;
}
/** Message snapshot plus the card session needed for later in-place updates. */
type MiniAppMessageResult = Message & {
    readonly miniAppCardSession: MiniAppCardSession;
};
interface MessagePart {
    /** Uploaded attachment guid for an attachment bubble. */
    readonly attachmentGuid?: string;
    /** Optional display name for the attachment bubble. */
    readonly attachmentName?: string;
    /** Server bubble index override for advanced multipart layouts. */
    readonly bubbleIndex?: number;
    /** UTF-16 formatting ranges that apply to this part's `text`. */
    readonly formatting?: readonly TextFormatInput[];
    /** Address represented by this text part when sending an `@` mention. */
    readonly mentionedAddress?: string;
    /** Text for a text or mention bubble. */
    readonly text?: string;
}
/**
 * Filter + paging knobs shared by `messages.listRecent` and `messages.listInChat`.
 *
 * Note: `chatGuid` is *not* on this type. `listInChat` takes it as its own
 * required argument so the type system rejects calling `listRecent({ chatGuid })`
 * (which the server silently ignores).
 */
interface MessageListFilter {
    /** Return messages created after this time. */
    readonly after?: Date;
    /** Return messages created before this time. */
    readonly before?: Date;
    /** Limit results to outgoing (`true`) or incoming (`false`) messages. */
    readonly isFromMe?: boolean;
    /** Limit results to read (`true`) or unread (`false`) messages. */
    readonly isRead?: boolean;
    /** Number of messages per page. The server currently requires `1..100`. */
    readonly pageSize?: number;
    /** Token returned by the previous page. */
    readonly pageToken?: string;
}
interface MessageListPage {
    readonly messages: readonly Message[];
    readonly nextPageToken?: string;
}
interface EmbeddedMediaItem {
    readonly data: Uint8Array;
    readonly mimeType: string;
}
type EmbeddedMedia = EmbeddedMediaItem;

/**
 * Poll-related domain types.
 */

interface PollOption {
    readonly creatorHandle?: string;
    readonly optionIdentifier: string;
    readonly text: string;
}
interface PollParticipantVote {
    readonly optionIdentifier: string;
    readonly participant: SingleServiceAddressInfo;
}
interface Poll {
    readonly chatGuid: string;
    readonly options: readonly PollOption[];
    readonly pollMessageGuid: string;
    readonly title: string;
    readonly votes: readonly PollParticipantVote[];
}
type PollChangeDelta = {
    readonly type: "created";
    readonly title: string;
    readonly options: readonly PollOption[];
} | {
    readonly type: "optionAdded";
    readonly title: string;
    readonly options: readonly PollOption[];
} | {
    readonly type: "voted";
    readonly optionIdentifier: string;
} | {
    readonly type: "unvoted";
    readonly optionIdentifier: string;
};

/**
 * Event types for live subscriptions and durable catch-up replay.
 */

interface EventContext {
    readonly actor?: SingleServiceAddressInfo;
    readonly chatGuid: string;
    readonly isFromMe: boolean;
    readonly occurredAt: Date;
}
type ChatEvent = (EventContext & {
    readonly type: "chat.backgroundChanged";
    readonly sequence: number;
}) | (EventContext & {
    readonly type: "chat.backgroundRemoved";
    readonly sequence: number;
}) | (EventContext & {
    readonly type: "chat.markedRead";
    readonly sequence: number;
}) | (EventContext & {
    readonly type: "chat.archived";
    readonly sequence: number;
}) | (EventContext & {
    readonly type: "chat.unarchived";
    readonly sequence: number;
});
type GroupChange = {
    readonly type: "displayNameChanged";
    readonly displayName: string;
} | {
    readonly type: "participantAdded";
    readonly participant: SingleServiceAddressInfo;
} | {
    readonly type: "participantRemoved";
    readonly participant: SingleServiceAddressInfo;
} | {
    readonly type: "participantLeft";
    readonly participant: SingleServiceAddressInfo;
} | {
    readonly type: "iconChanged";
} | {
    readonly type: "iconRemoved";
};
interface GroupEvent extends EventContext {
    readonly change: GroupChange;
    readonly sequence: number;
    readonly type: "group.changed";
}
type MessageEvent = (EventContext & {
    readonly type: "message.received";
    readonly sequence: number;
    readonly message: Message;
}) | (EventContext & {
    readonly type: "message.edited";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly content: Message["content"];
    readonly editedAt: Date;
}) | (EventContext & {
    readonly type: "message.read";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly readAt: Date;
}) | (EventContext & {
    readonly type: "message.unsent";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly retractedAt: Date;
}) | (EventContext & {
    readonly type: "message.reactionAdded";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly reaction: MessageReaction;
    readonly targetPartIndex?: number;
}) | (EventContext & {
    readonly type: "message.reactionRemoved";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly reaction: MessageReaction;
    readonly targetPartIndex?: number;
}) | (EventContext & {
    readonly type: "message.stickerPlaced";
    readonly sequence: number;
    readonly messageGuid: string;
    readonly sticker?: Message["placedStickers"][number]["sticker"];
    readonly placement?: StickerPlacement;
    readonly targetPartIndex?: number;
});
interface PollEvent extends EventContext {
    readonly delta: PollChangeDelta;
    readonly pollMessageGuid: string;
    readonly sequence: number;
    readonly type: "poll.changed";
}
type LiveEvent = MessageEvent | ChatEvent | GroupEvent | PollEvent;
type CatchUpEvent = LiveEvent | {
    readonly type: "catchup.complete";
    readonly headSequence: number;
};
interface EventTypeMap {
    "chat.archived": Extract<ChatEvent, {
        type: "chat.archived";
    }>;
    "chat.backgroundChanged": Extract<ChatEvent, {
        type: "chat.backgroundChanged";
    }>;
    "chat.backgroundRemoved": Extract<ChatEvent, {
        type: "chat.backgroundRemoved";
    }>;
    "chat.markedRead": Extract<ChatEvent, {
        type: "chat.markedRead";
    }>;
    "chat.unarchived": Extract<ChatEvent, {
        type: "chat.unarchived";
    }>;
    "group.changed": GroupEvent;
    "message.edited": Extract<MessageEvent, {
        type: "message.edited";
    }>;
    "message.reactionAdded": Extract<MessageEvent, {
        type: "message.reactionAdded";
    }>;
    "message.reactionRemoved": Extract<MessageEvent, {
        type: "message.reactionRemoved";
    }>;
    "message.read": Extract<MessageEvent, {
        type: "message.read";
    }>;
    "message.received": Extract<MessageEvent, {
        type: "message.received";
    }>;
    "message.stickerPlaced": Extract<MessageEvent, {
        type: "message.stickerPlaced";
    }>;
    "message.unsent": Extract<MessageEvent, {
        type: "message.unsent";
    }>;
    "poll.changed": PollEvent;
}
type EventType = keyof EventTypeMap;

/**
 * Parse proto3-JSON `photon.imessage.v1.*ChangeEvent` payloads into the
 * SDK's public event types.
 *
 * Inbound events no longer stream through this client (they ride Fusor);
 * they arrive as proto3-JSON bodies inside delivery envelopes. These
 * helpers are the single source of truth for decoding them: the generated
 * codecs own the wire conventions (camelCase fields, RFC3339 timestamps →
 * `Date`, enum names, base64 bytes) and the same mappers the old streaming
 * transport used produce the public shapes — so consumers never hand-write
 * a schema for the event graph.
 *
 * `sequence` is the envelope's per-line sequence, stamped onto the event
 * exactly as the streaming plane once did.
 *
 * Returns `undefined` when the payload decodes but carries no change this
 * SDK version recognizes (e.g. a newer server's new event variant) —
 * callers should skip those, not throw. A structurally invalid payload
 * (wrong field types, malformed timestamps) throws.
 */
declare function parseMessageChangeEvent(json: unknown, sequence: number): MessageEvent | undefined;
/** See {@link parseMessageChangeEvent}. */
declare function parsePollChangeEvent(json: unknown, sequence: number): PollEvent | undefined;
/** See {@link parseMessageChangeEvent}. */
declare function parseGroupChangeEvent(json: unknown, sequence: number): GroupEvent | undefined;
/** See {@link parseMessageChangeEvent}. */
declare function parseChatChangeEvent(json: unknown, sequence: number): ChatEvent | undefined;
/**
 * Decode one binary protobuf `CatchUpEventsResponse` frame into the same
 * high-level event shape the v1 `events.catchUp()` stream produced.
 *
 * Heartbeats, sequence-only frames, and frames without a recognized event
 * payload return `undefined`. Errors from the generated protobuf decoder
 * propagate to the caller.
 */
declare function decodeCatchUpEvent(bytes: Uint8Array): CatchUpEvent | undefined;

/**
 * Chat-related domain types.
 */

/** A conversation (direct or group) in iMessage. */
interface Chat {
    readonly chatIdentifier?: string;
    readonly displayName: string;
    readonly groupId?: string;
    readonly guid: string;
    readonly isArchived: boolean;
    readonly isFiltered: boolean;
    readonly isGroup: boolean;
    readonly lastMessage?: Message;
    readonly participants: readonly SingleServiceAddressInfo[];
    readonly service: ChatServiceType;
    readonly unreadCount?: number;
}
interface CreateChatOptions {
    /** Pre-rendered attributed body for the opening message, when supplied. */
    readonly attributedBody?: Uint8Array;
    /** Caller-generated idempotency key for chat creation and the optional opening send. */
    readonly clientMessageId?: string;
    /** Full-screen or bubble effect for `message`. */
    readonly effect?: MessageEffect;
    /** Optional opening text sent in the same server call that creates the chat. */
    readonly message?: string;
    /** Optional subject line for the opening message. */
    readonly subject?: string;
}
/**
 * Result of `chats.create`.
 */
interface CreateChatResult {
    readonly chat: Chat;
    readonly initialMessage?: Message;
}

/**
 * Shared friend location domain types.
 */
interface SharedFriendLocation {
    readonly accuracy?: number;
    readonly address: string;
    readonly expiresAt?: Date;
    readonly isLocatingInProgress: boolean;
    readonly latitude?: number;
    readonly locationTimestamp?: Date;
    readonly locationType: "legacy" | "live" | "shallow" | "unknown";
    readonly longAddress?: string;
    readonly longitude?: number;
    readonly name?: string;
    readonly shortAddress?: string;
}
interface SharedFriendLocationUpdated {
    readonly location: SharedFriendLocation;
    readonly sourceSequence: number;
}
interface LocationRequestReceipt {
    readonly address: string;
    readonly messageGuid?: string;
    readonly reason?: string;
    readonly status: string;
}

/**
 * Core streaming primitive used by every server-streaming SDK method.
 *
 * Wraps an async iterable source (typically from a gRPC server-streaming RPC)
 * Exposes `for await`, `.on()`, `.filter()`, `.map()`, `.take()`, `.close()`,
 * and `Symbol.asyncDispose`.
 *
 * Only one consumer is allowed per stream instance. Calling `.on()` or
 * iterating a stream that is already being consumed throws an error.
 * Use `.filter()` / `.map()` / `.take()` to derive new streams before
 * consuming them.
 */
declare class TypedEventStream<T> implements AsyncIterable<T>, AsyncDisposable {
    /** The underlying async iterable source. */
    private readonly _source;
    /** Optional cleanup callback invoked when the stream is closed. */
    private readonly _cleanup;
    /** Whether this stream has been closed. */
    private _closed;
    /** Whether a consumer has already attached to this stream. */
    private _consumed;
    /**
     * When a consumer is blocked on `next()`, this resolver lets `.close()`
     * interrupt it immediately.
     */
    private _cancelResolve;
    constructor(source: AsyncIterable<T>, cleanup?: () => Promise<void>);
    [Symbol.asyncIterator](): AsyncIterator<T>;
    /**
     * Subscribe to events with a callback. Returns an unsubscribe function that
     * stops the internal consumption loop.
     *
     * The callback may be synchronous or async. If async, back-pressure is
     * applied -- the next event is not delivered until the previous callback
     * resolves. Pass `onError` to handle callback or source errors; otherwise
     * errors are rethrown on the next microtask.
     */
    on(callback: (event: T) => void | Promise<void>, onError?: (error: unknown) => void): () => void;
    /**
     * Create a filtered sub-stream. When the predicate is a type-guard the
     * returned stream is narrowed to the guard type.
     */
    filter<S extends T>(predicate: (event: T) => event is S): TypedEventStream<S>;
    filter(predicate: (event: T) => boolean): TypedEventStream<T>;
    /** Transform each event into a different shape. */
    map<U>(transform: (event: T) => U): TypedEventStream<U>;
    /**
     * Yield the first `count` events, then close the parent stream.
     *
     * `count <= 0` returns an empty stream without consuming the parent.
     */
    take(count: number): TypedEventStream<T>;
    /** Signal the stream is done. Interrupts any pending iteration. */
    close(): Promise<void>;
    /** `Symbol.asyncDispose` -- enables `await using`. */
    [Symbol.asyncDispose](): Promise<void>;
    /**
     * Claim sole-consumer rights. Throws if the stream has already been
     * consumed or closed.
     */
    private _claimConsumer;
    /**
     * Internal async generator that pulls from the source and respects the
     * closed flag.
     */
    private _iterate;
    /**
     * Returns a promise that resolves to `undefined` when `.close()` is called.
     * If the stream is already closed, resolves immediately.
     */
    private _cancelPromise;
}

/**
 * Shared types used across multiple resource namespaces.
 */
/** Options for automatic retry with exponential back-off. */
interface RetryOptions {
    /** Initial delay in milliseconds before the first retry. */
    readonly initialDelay?: number;
    /** Maximum number of attempts (including the initial call). */
    readonly maxAttempts?: number;
    /** Maximum delay in milliseconds between retries. */
    readonly maxDelay?: number;
}
/** Caller-provided key that lets the server deduplicate retries. */
interface IdempotencyOptions {
    /** Stable key for one logical write. Reuse it only when retrying that same write. */
    readonly clientMessageId?: string;
}
/**
 * Invoked once for every server heartbeat frame received on any
 * `subscribeEvents` / `watch` stream.
 *
 * The server emits heartbeats on a fixed cadence (~30s) even when no domain
 * events occur. Consumers can use this signal to run a stall watchdog: if no
 * heartbeat arrives within a grace window, the connection is likely half-open
 * and the stream should be torn down and re-established. Without this hook the
 * heartbeat frames are silently dropped and a half-open stall is undetectable
 * from the event stream alone.
 */
type HeartbeatHandler = () => void;

/** Group photo bytes and MIME type returned by `groups.getIcon`. */
interface GroupIcon {
    readonly data: Uint8Array;
    readonly mimeType: string;
}

export { type SharedFriendLocation as $, type AttachmentInfo as A, type MessagePart as B, type CatchUpEvent as C, type DownloadAttachmentChunk as D, type EmbeddedMedia as E, type MessagePlacedSticker as F, type GroupChange as G, type MessageReaction as H, IMessageError as I, type MiniAppCardSession as J, type MiniAppContent as K, type LiveEvent as L, type Message as M, type MiniAppLayout as N, type MiniAppLayoutInfo as O, type MiniAppMessageResult as P, type MultiServiceAddressInfo as Q, NotFoundError as R, type Poll as S, type PollChangeDelta as T, type PollEvent as U, type PollOption as V, type PollParticipantVote as W, RateLimitError as X, type RetryOptions as Y, type SendOptions as Z, type SettableMessageReaction as _, type AttachmentInput as a, type SharedFriendLocationUpdated as a0, type SingleServiceAddressInfo as a1, type StickerPlacement as a2, TextEffect as a3, type TextFormat as a4, type TextFormatInput as a5, type TransferState as a6, TypedEventStream as a7, type UploadAttachmentResult as a8, ValidationError as a9, decodeCatchUpEvent as aa, parseChatChangeEvent as ab, parseGroupChangeEvent as ac, parseMessageChangeEvent as ad, parsePollChangeEvent as ae, type HeartbeatHandler as af, AuthenticationError as b, type Chat as c, type ChatEvent as d, type ChatServiceType as e, type CompanionInfo as f, ConnectionError as g, type CreateChatOptions as h, type CreateChatResult as i, type CustomizedMiniAppMessage as j, type EmbeddedMediaItem as k, ErrorCode as l, type EventType as m, type EventTypeMap as n, type GroupEvent as o, type GroupIcon as p, type IdempotencyOptions as q, type LocationRequestReceipt as r, type MessageAppliedReaction as s, type MessageContent as t, MessageEffect as u, type MessageEvent as v, type MessageItemType as w, type MessageListFilter as x, type MessageListPage as y, type MessageMention as z };
