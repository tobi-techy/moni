import { MultiServiceAddressInfo, AttachmentInfo, Companion, CompanionInfo, chatServiceTypeToJSON, chatServiceTypeFromJSON, Chat, Message, ChatChangeEvent, Heartbeat, GroupChangeEvent, SharedFriendLocation, SharedFriendLocationUpdated, ReplyTarget, TextFormat, AttachmentRef, MessagePart, MessageReaction, StickerPlacement, Timestamp, EmbeddedMedia, MessageChangeEvent, PollInfo, PollChangeEvent } from './chunk-KA2VVWQX.js';
import { BinaryWriter, BinaryReader } from '@bufbuild/protobuf/wire';

function createBaseGetAddressInfoRequest() {
  return { address: "" };
}
var GetAddressInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAddressInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetAddressInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAddressInfoRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetAddressInfoResponse() {
  return { info: void 0 };
}
var GetAddressInfoResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.info !== void 0) {
      MultiServiceAddressInfo.encode(message.info, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAddressInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.info = MultiServiceAddressInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { info: isSet(object.info) ? MultiServiceAddressInfo.fromJSON(object.info) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.info !== void 0) {
      obj.info = MultiServiceAddressInfo.toJSON(message.info);
    }
    return obj;
  },
  create(base) {
    return GetAddressInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAddressInfoResponse();
    message.info = object.info !== void 0 && object.info !== null ? MultiServiceAddressInfo.fromPartial(object.info) : void 0;
    return message;
  }
};
function createBaseGetFocusStatusRequest() {
  return { address: "" };
}
var GetFocusStatusRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetFocusStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetFocusStatusRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetFocusStatusRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetFocusStatusResponse() {
  return { isSilencedByFocus: false };
}
var GetFocusStatusResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.isSilencedByFocus !== false) {
      writer.uint32(8).bool(message.isSilencedByFocus);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetFocusStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.isSilencedByFocus = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      isSilencedByFocus: isSet(object.isSilencedByFocus) ? globalThis.Boolean(object.isSilencedByFocus) : isSet(object.is_silenced_by_focus) ? globalThis.Boolean(object.is_silenced_by_focus) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.isSilencedByFocus !== false) {
      obj.isSilencedByFocus = message.isSilencedByFocus;
    }
    return obj;
  },
  create(base) {
    return GetFocusStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetFocusStatusResponse();
    message.isSilencedByFocus = object.isSilencedByFocus ?? false;
    return message;
  }
};
function createBaseGetIMessageAvailabilityRequest() {
  return { address: "" };
}
var GetIMessageAvailabilityRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIMessageAvailabilityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetIMessageAvailabilityRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIMessageAvailabilityRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetIMessageAvailabilityResponse() {
  return { isAvailable: false };
}
var GetIMessageAvailabilityResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.isAvailable !== false) {
      writer.uint32(8).bool(message.isAvailable);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIMessageAvailabilityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.isAvailable = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      isAvailable: isSet(object.isAvailable) ? globalThis.Boolean(object.isAvailable) : isSet(object.is_available) ? globalThis.Boolean(object.is_available) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.isAvailable !== false) {
      obj.isAvailable = message.isAvailable;
    }
    return obj;
  },
  create(base) {
    return GetIMessageAvailabilityResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIMessageAvailabilityResponse();
    message.isAvailable = object.isAvailable ?? false;
    return message;
  }
};
var AddressServiceDefinition = {
  name: "AddressService",
  fullName: "photon.imessage.v1.AddressService",
  methods: {
    /**
     * Returns services seen and country for the address.
     *
     * Errors: NOT_FOUND when the address has never been seen in this device's
     * iMessage history.
     *
     * Over HTTP, `address` is a query parameter (addresses contain `+`, which
     * is path-hostile) — remember to URL-encode it (`%2B`).
     */
    getAddressInfo: {
      name: "GetAddressInfo",
      requestType: GetAddressInfoRequest,
      requestStream: false,
      responseType: GetAddressInfoResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              20,
              18,
              18,
              47,
              118,
              49,
              47,
              97,
              100,
              100,
              114,
              101,
              115,
              115,
              101,
              115,
              58,
              105,
              110,
              102,
              111
            ])
          ]
        }
      }
    },
    /** Returns whether Messages currently reports this address as Focus-silenced. */
    getFocusStatus: {
      name: "GetFocusStatus",
      requestType: GetFocusStatusRequest,
      requestStream: false,
      responseType: GetFocusStatusResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              27,
              18,
              25,
              47,
              118,
              49,
              47,
              97,
              100,
              100,
              114,
              101,
              115,
              115,
              101,
              115,
              58,
              102,
              111,
              99,
              117,
              115,
              83,
              116,
              97,
              116,
              117,
              115
            ])
          ]
        }
      }
    },
    /** Returns whether the address is reachable via iMessage at query time. */
    getIMessageAvailability: {
      name: "GetIMessageAvailability",
      requestType: GetIMessageAvailabilityRequest,
      requestStream: false,
      responseType: GetIMessageAvailabilityResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              36,
              18,
              34,
              47,
              118,
              49,
              47,
              97,
              100,
              100,
              114,
              101,
              115,
              115,
              101,
              115,
              58,
              105,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              65,
              118,
              97,
              105,
              108,
              97,
              98,
              105,
              108,
              105,
              116,
              121
            ])
          ]
        }
      }
    }
  }
};
function isSet(value) {
  return value !== null && value !== void 0;
}
function createBaseGetAttachmentInfoRequest() {
  return { attachmentGuid: "" };
}
var GetAttachmentInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachmentGuid !== "") {
      writer.uint32(10).string(message.attachmentGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAttachmentInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachmentGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachmentGuid: isSet2(object.attachmentGuid) ? globalThis.String(object.attachmentGuid) : isSet2(object.attachment_guid) ? globalThis.String(object.attachment_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachmentGuid !== "") {
      obj.attachmentGuid = message.attachmentGuid;
    }
    return obj;
  },
  create(base) {
    return GetAttachmentInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAttachmentInfoRequest();
    message.attachmentGuid = object.attachmentGuid ?? "";
    return message;
  }
};
function createBaseGetAttachmentInfoResponse() {
  return { attachment: void 0 };
}
var GetAttachmentInfoResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetAttachmentInfoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { attachment: isSet2(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    return obj;
  },
  create(base) {
    return GetAttachmentInfoResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetAttachmentInfoResponse();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    return message;
  }
};
function createBaseUploadAttachmentRequest() {
  return { fileName: "", data: new Uint8Array(0), companion: void 0 };
}
var UploadAttachmentRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.fileName !== "") {
      writer.uint32(10).string(message.fileName);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.companion !== void 0) {
      Companion.encode(message.companion, writer.uint32(26).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.fileName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.companion = Companion.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      fileName: isSet2(object.fileName) ? globalThis.String(object.fileName) : isSet2(object.file_name) ? globalThis.String(object.file_name) : "",
      data: isSet2(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      companion: isSet2(object.companion) ? Companion.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.companion !== void 0) {
      obj.companion = Companion.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return UploadAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadAttachmentRequest();
    message.fileName = object.fileName ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.companion = object.companion !== void 0 && object.companion !== null ? Companion.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseUploadAttachmentResponse() {
  return { attachment: void 0, companion: void 0 };
}
var UploadAttachmentResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    if (message.companion !== void 0) {
      CompanionInfo.encode(message.companion, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUploadAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.companion = CompanionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachment: isSet2(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0,
      companion: isSet2(object.companion) ? CompanionInfo.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    if (message.companion !== void 0) {
      obj.companion = CompanionInfo.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return UploadAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUploadAttachmentResponse();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    message.companion = object.companion !== void 0 && object.companion !== null ? CompanionInfo.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseDownloadAttachmentRequest() {
  return { attachmentGuid: "" };
}
var DownloadAttachmentRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachmentGuid !== "") {
      writer.uint32(10).string(message.attachmentGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadAttachmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachmentGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachmentGuid: isSet2(object.attachmentGuid) ? globalThis.String(object.attachmentGuid) : isSet2(object.attachment_guid) ? globalThis.String(object.attachment_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachmentGuid !== "") {
      obj.attachmentGuid = message.attachmentGuid;
    }
    return obj;
  },
  create(base) {
    return DownloadAttachmentRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadAttachmentRequest();
    message.attachmentGuid = object.attachmentGuid ?? "";
    return message;
  }
};
function createBaseDownloadHeader() {
  return { attachment: void 0, companion: void 0 };
}
var DownloadHeader = {
  encode(message, writer = new BinaryWriter()) {
    if (message.attachment !== void 0) {
      AttachmentInfo.encode(message.attachment, writer.uint32(10).fork()).join();
    }
    if (message.companion !== void 0) {
      CompanionInfo.encode(message.companion, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.attachment = AttachmentInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.companion = CompanionInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      attachment: isSet2(object.attachment) ? AttachmentInfo.fromJSON(object.attachment) : void 0,
      companion: isSet2(object.companion) ? CompanionInfo.fromJSON(object.companion) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentInfo.toJSON(message.attachment);
    }
    if (message.companion !== void 0) {
      obj.companion = CompanionInfo.toJSON(message.companion);
    }
    return obj;
  },
  create(base) {
    return DownloadHeader.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadHeader();
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentInfo.fromPartial(object.attachment) : void 0;
    message.companion = object.companion !== void 0 && object.companion !== null ? CompanionInfo.fromPartial(object.companion) : void 0;
    return message;
  }
};
function createBaseDownloadAttachmentResponse() {
  return { header: void 0, primaryChunk: void 0, companionChunk: void 0 };
}
var DownloadAttachmentResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.header !== void 0) {
      DownloadHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.primaryChunk !== void 0) {
      writer.uint32(18).bytes(message.primaryChunk);
    }
    if (message.companionChunk !== void 0) {
      writer.uint32(26).bytes(message.companionChunk);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseDownloadAttachmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.header = DownloadHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.primaryChunk = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.companionChunk = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      header: isSet2(object.header) ? DownloadHeader.fromJSON(object.header) : void 0,
      primaryChunk: isSet2(object.primaryChunk) ? bytesFromBase64(object.primaryChunk) : isSet2(object.primary_chunk) ? bytesFromBase64(object.primary_chunk) : void 0,
      companionChunk: isSet2(object.companionChunk) ? bytesFromBase64(object.companionChunk) : isSet2(object.companion_chunk) ? bytesFromBase64(object.companion_chunk) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.header !== void 0) {
      obj.header = DownloadHeader.toJSON(message.header);
    }
    if (message.primaryChunk !== void 0) {
      obj.primaryChunk = base64FromBytes(message.primaryChunk);
    }
    if (message.companionChunk !== void 0) {
      obj.companionChunk = base64FromBytes(message.companionChunk);
    }
    return obj;
  },
  create(base) {
    return DownloadAttachmentResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseDownloadAttachmentResponse();
    message.header = object.header !== void 0 && object.header !== null ? DownloadHeader.fromPartial(object.header) : void 0;
    message.primaryChunk = object.primaryChunk ?? void 0;
    message.companionChunk = object.companionChunk ?? void 0;
    return message;
  }
};
var AttachmentServiceDefinition = {
  name: "AttachmentService",
  fullName: "photon.imessage.v1.AttachmentService",
  methods: {
    /** Metadata-only lookup. Cheap; never touches attachment bytes. */
    getAttachmentInfo: {
      name: "GetAttachmentInfo",
      requestType: GetAttachmentInfoRequest,
      requestStream: false,
      responseType: GetAttachmentInfoResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              35,
              18,
              33,
              47,
              118,
              49,
              47,
              97,
              116,
              116,
              97,
              99,
              104,
              109,
              101,
              110,
              116,
              115,
              47,
              123,
              97,
              116,
              116,
              97,
              99,
              104,
              109,
              101,
              110,
              116,
              95,
              103,
              117,
              105,
              100,
              125
            ])
          ]
        }
      }
    },
    /**
     * Atomic upload of one primary file plus an optional sidecar. The
     * server persists both before responding; on failure neither is visible
     * to subsequent reads.
     *
     * No HTTP mapping on purpose: attachment payloads don't belong
     * base64-encoded in JSON. Served over HTTP by a dedicated raw-bytes
     * route in the transcoding middleware (as is `DownloadAttachment`).
     */
    uploadAttachment: {
      name: "UploadAttachment",
      requestType: UploadAttachmentRequest,
      requestStream: false,
      responseType: UploadAttachmentResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Server-streaming bytes. See `DownloadAttachmentResponse` for the
     * frame contract.
     */
    downloadAttachment: {
      name: "DownloadAttachment",
      requestType: DownloadAttachmentRequest,
      requestStream: false,
      responseType: DownloadAttachmentResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase64(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function isSet2(value) {
  return value !== null && value !== void 0;
}
function createBaseEmpty() {
  return {};
}
var Empty = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEmpty();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return Empty.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseEmpty();
    return message;
  }
};

// ../core/src/generated/photon/imessage/v1/chat_service.ts
function createBaseCreateChatRequest() {
  return { addresses: [], service: 0, initialMessage: void 0, clientMessageId: void 0 };
}
var CreateChatRequest = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.addresses) {
      writer.uint32(10).string(v);
    }
    if (message.service !== 0) {
      writer.uint32(16).int32(message.service);
    }
    if (message.initialMessage !== void 0) {
      CreateChatInitialMessage.encode(message.initialMessage, writer.uint32(26).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.service = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.initialMessage = CreateChatInitialMessage.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      service: isSet3(object.service) ? chatServiceTypeFromJSON(object.service) : 0,
      initialMessage: isSet3(object.initialMessage) ? CreateChatInitialMessage.fromJSON(object.initialMessage) : isSet3(object.initial_message) ? CreateChatInitialMessage.fromJSON(object.initial_message) : void 0,
      clientMessageId: isSet3(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet3(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.service !== 0) {
      obj.service = chatServiceTypeToJSON(message.service);
    }
    if (message.initialMessage !== void 0) {
      obj.initialMessage = CreateChatInitialMessage.toJSON(message.initialMessage);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return CreateChatRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatRequest();
    message.addresses = object.addresses?.map((e) => e) || [];
    message.service = object.service ?? 0;
    message.initialMessage = object.initialMessage !== void 0 && object.initialMessage !== null ? CreateChatInitialMessage.fromPartial(object.initialMessage) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseCreateChatInitialMessage() {
  return { text: "", attributedBody: void 0, effectId: void 0, subject: void 0 };
}
var CreateChatInitialMessage = {
  encode(message, writer = new BinaryWriter()) {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.attributedBody !== void 0) {
      writer.uint32(18).bytes(message.attributedBody);
    }
    if (message.effectId !== void 0) {
      writer.uint32(26).string(message.effectId);
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatInitialMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attributedBody = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      text: isSet3(object.text) ? globalThis.String(object.text) : "",
      attributedBody: isSet3(object.attributedBody) ? bytesFromBase642(object.attributedBody) : isSet3(object.attributed_body) ? bytesFromBase642(object.attributed_body) : void 0,
      effectId: isSet3(object.effectId) ? globalThis.String(object.effectId) : isSet3(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      subject: isSet3(object.subject) ? globalThis.String(object.subject) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.attributedBody !== void 0) {
      obj.attributedBody = base64FromBytes2(message.attributedBody);
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    return obj;
  },
  create(base) {
    return CreateChatInitialMessage.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatInitialMessage();
    message.text = object.text ?? "";
    message.attributedBody = object.attributedBody ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.subject = object.subject ?? void 0;
    return message;
  }
};
function createBaseCreateChatResponse() {
  return { chat: void 0, initialMessage: void 0 };
}
var CreateChatResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    if (message.initialMessage !== void 0) {
      Message.encode(message.initialMessage, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreateChatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.initialMessage = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chat: isSet3(object.chat) ? Chat.fromJSON(object.chat) : void 0,
      initialMessage: isSet3(object.initialMessage) ? Message.fromJSON(object.initialMessage) : isSet3(object.initial_message) ? Message.fromJSON(object.initial_message) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    if (message.initialMessage !== void 0) {
      obj.initialMessage = Message.toJSON(message.initialMessage);
    }
    return obj;
  },
  create(base) {
    return CreateChatResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreateChatResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    message.initialMessage = object.initialMessage !== void 0 && object.initialMessage !== null ? Message.fromPartial(object.initialMessage) : void 0;
    return message;
  }
};
function createBaseMarkChatReadRequest() {
  return { chatGuid: "" };
}
var MarkChatReadRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMarkChatReadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return MarkChatReadRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMarkChatReadRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseSetBackgroundRequest() {
  return { chatGuid: "", data: new Uint8Array(0) };
}
var SetBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      data: isSet3(object.data) ? bytesFromBase642(object.data) : new Uint8Array(0)
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes2(message.data);
    }
    return obj;
  },
  create(base) {
    return SetBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.data = object.data ?? new Uint8Array(0);
    return message;
  }
};
function createBaseRemoveBackgroundRequest() {
  return { chatGuid: "" };
}
var RemoveBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return RemoveBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseShareContactInfoRequest() {
  return { chatGuid: "" };
}
var ShareContactInfoRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseShareContactInfoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return ShareContactInfoRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseShareContactInfoRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseSetTypingRequest() {
  return { chatGuid: "", isTyping: false };
}
var SetTypingRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.isTyping !== false) {
      writer.uint32(16).bool(message.isTyping);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetTypingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.isTyping = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      isTyping: isSet3(object.isTyping) ? globalThis.Boolean(object.isTyping) : isSet3(object.is_typing) ? globalThis.Boolean(object.is_typing) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.isTyping !== false) {
      obj.isTyping = message.isTyping;
    }
    return obj;
  },
  create(base) {
    return SetTypingRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetTypingRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.isTyping = object.isTyping ?? false;
    return message;
  }
};
function createBaseGetChatRequest() {
  return { chatGuid: "" };
}
var GetChatRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return GetChatRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseGetChatResponse() {
  return { chat: void 0 };
}
var GetChatResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet3(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return GetChatResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseGetChatCountRequest() {
  return { includeArchived: false };
}
var GetChatCountRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.includeArchived !== false) {
      writer.uint32(8).bool(message.includeArchived);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatCountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.includeArchived = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      includeArchived: isSet3(object.includeArchived) ? globalThis.Boolean(object.includeArchived) : isSet3(object.include_archived) ? globalThis.Boolean(object.include_archived) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.includeArchived !== false) {
      obj.includeArchived = message.includeArchived;
    }
    return obj;
  },
  create(base) {
    return GetChatCountRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatCountRequest();
    message.includeArchived = object.includeArchived ?? false;
    return message;
  }
};
function createBaseGetChatCountResponse() {
  return { count: 0 };
}
var GetChatCountResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.count !== 0) {
      writer.uint32(8).int64(message.count);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetChatCountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { count: isSet3(object.count) ? globalThis.Number(object.count) : 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },
  create(base) {
    return GetChatCountResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetChatCountResponse();
    message.count = object.count ?? 0;
    return message;
  }
};
function createBaseHasBackgroundRequest() {
  return { chatGuid: "" };
}
var HasBackgroundRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHasBackgroundRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return HasBackgroundRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseHasBackgroundRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseHasBackgroundResponse() {
  return { backgroundPresent: false };
}
var HasBackgroundResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.backgroundPresent !== false) {
      writer.uint32(8).bool(message.backgroundPresent);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseHasBackgroundResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.backgroundPresent = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      backgroundPresent: isSet3(object.backgroundPresent) ? globalThis.Boolean(object.backgroundPresent) : isSet3(object.background_present) ? globalThis.Boolean(object.background_present) : false
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.backgroundPresent !== false) {
      obj.backgroundPresent = message.backgroundPresent;
    }
    return obj;
  },
  create(base) {
    return HasBackgroundResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseHasBackgroundResponse();
    message.backgroundPresent = object.backgroundPresent ?? false;
    return message;
  }
};
function createBaseSubscribeChatEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeChatEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeChatEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet3(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet3(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeChatEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeChatEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeChatEventsResponse() {
  return { sequence: void 0, chatChanged: void 0, heartbeat: void 0 };
}
var SubscribeChatEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.chatChanged !== void 0) {
      ChatChangeEvent.encode(message.chatChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeChatEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.chatChanged = ChatChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet3(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      chatChanged: isSet3(object.chatChanged) ? ChatChangeEvent.fromJSON(object.chatChanged) : isSet3(object.chat_changed) ? ChatChangeEvent.fromJSON(object.chat_changed) : void 0,
      heartbeat: isSet3(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.chatChanged !== void 0) {
      obj.chatChanged = ChatChangeEvent.toJSON(message.chatChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeChatEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeChatEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.chatChanged = object.chatChanged !== void 0 && object.chatChanged !== null ? ChatChangeEvent.fromPartial(object.chatChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var ChatServiceDefinition = {
  name: "ChatService",
  fullName: "photon.imessage.v1.ChatService",
  methods: {
    /** Writes */
    createChat: {
      name: "CreateChat",
      requestType: CreateChatRequest,
      requestStream: false,
      responseType: CreateChatResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([14, 58, 1, 42, 34, 9, 47, 118, 49, 47, 99, 104, 97, 116, 115])]
        }
      }
    },
    markChatRead: {
      name: "MarkChatRead",
      requestType: MarkChatReadRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              23,
              58,
              1,
              42,
              34,
              18,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              109,
              97,
              114,
              107,
              82,
              101,
              97,
              100
            ])
          ]
        }
      }
    },
    /**
     * Over HTTP, `data` rides base64-encoded in the JSON body (proto3-JSON
     * `bytes` representation).
     */
    setBackground: {
      name: "SetBackground",
      requestType: SetBackgroundRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              28,
              58,
              1,
              42,
              34,
              23,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              115,
              101,
              116,
              66,
              97,
              99,
              107,
              103,
              114,
              111,
              117,
              110,
              100
            ])
          ]
        }
      }
    },
    removeBackground: {
      name: "RemoveBackground",
      requestType: RemoveBackgroundRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              31,
              58,
              1,
              42,
              34,
              26,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              114,
              101,
              109,
              111,
              118,
              101,
              66,
              97,
              99,
              107,
              103,
              114,
              111,
              117,
              110,
              100
            ])
          ]
        }
      }
    },
    /**
     * Pushes the local user's name-and-photo card into the target chat so
     * the recipient sees it in their contact suggestions. No observable
     * local state change.
     */
    shareContactInfo: {
      name: "ShareContactInfo",
      requestType: ShareContactInfoRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              31,
              58,
              1,
              42,
              34,
              26,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              115,
              104,
              97,
              114,
              101,
              67,
              111,
              110,
              116,
              97,
              99,
              116,
              73,
              110,
              102,
              111
            ])
          ]
        }
      }
    },
    /** Transient typing-indicator write. No persisted state. */
    setTyping: {
      name: "SetTyping",
      requestType: SetTypingRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              24,
              58,
              1,
              42,
              34,
              19,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              115,
              101,
              116,
              84,
              121,
              112,
              105,
              110,
              103
            ])
          ]
        }
      }
    },
    /**
     * Reads (chat_guid is path-hostile, so these are :customVerb GETs with
     * query parameters rather than resource paths).
     */
    getChat: {
      name: "GetChat",
      requestType: GetChatRequest,
      requestStream: false,
      responseType: GetChatResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([15, 18, 13, 47, 118, 49, 47, 99, 104, 97, 116, 115, 58, 103, 101, 116])
          ]
        }
      }
    },
    getChatCount: {
      name: "GetChatCount",
      requestType: GetChatCountRequest,
      requestStream: false,
      responseType: GetChatCountResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              17,
              18,
              15,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              99,
              111,
              117,
              110,
              116
            ])
          ]
        }
      }
    },
    hasBackground: {
      name: "HasBackground",
      requestType: HasBackgroundRequest,
      requestStream: false,
      responseType: HasBackgroundResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              25,
              18,
              23,
              47,
              118,
              49,
              47,
              99,
              104,
              97,
              116,
              115,
              58,
              104,
              97,
              115,
              66,
              97,
              99,
              107,
              103,
              114,
              111,
              117,
              110,
              100
            ])
          ]
        }
      }
    },
    subscribeChatEvents: {
      name: "SubscribeChatEvents",
      requestType: SubscribeChatEventsRequest,
      requestStream: false,
      responseType: SubscribeChatEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase642(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes2(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet3(value) {
  return value !== null && value !== void 0;
}
function createBaseSetDisplayNameRequest() {
  return { chatGuid: "", displayName: "", clientMessageId: void 0 };
}
var SetDisplayNameRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetDisplayNameRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.displayName = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      displayName: isSet4(object.displayName) ? globalThis.String(object.displayName) : isSet4(object.display_name) ? globalThis.String(object.display_name) : "",
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetDisplayNameRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetDisplayNameRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.displayName = object.displayName ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetDisplayNameResponse() {
  return { chat: void 0 };
}
var SetDisplayNameResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetDisplayNameResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet4(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return SetDisplayNameResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetDisplayNameResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseAddParticipantsRequest() {
  return { chatGuid: "", addresses: [], clientMessageId: void 0 };
}
var AddParticipantsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.addresses) {
      writer.uint32(18).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddParticipantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return AddParticipantsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddParticipantsRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.addresses = object.addresses?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseAddParticipantsResponse() {
  return { chat: void 0 };
}
var AddParticipantsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddParticipantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet4(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return AddParticipantsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddParticipantsResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseRemoveParticipantsRequest() {
  return { chatGuid: "", addresses: [], clientMessageId: void 0 };
}
var RemoveParticipantsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.addresses) {
      writer.uint32(18).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveParticipantsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.addresses.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      addresses: globalThis.Array.isArray(object?.addresses) ? object.addresses.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RemoveParticipantsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveParticipantsRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.addresses = object.addresses?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRemoveParticipantsResponse() {
  return { chat: void 0 };
}
var RemoveParticipantsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chat !== void 0) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveParticipantsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { chat: isSet4(object.chat) ? Chat.fromJSON(object.chat) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.chat !== void 0) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },
  create(base) {
    return RemoveParticipantsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveParticipantsResponse();
    message.chat = object.chat !== void 0 && object.chat !== null ? Chat.fromPartial(object.chat) : void 0;
    return message;
  }
};
function createBaseLeaveGroupRequest() {
  return { chatGuid: "", clientMessageId: void 0 };
}
var LeaveGroupRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseLeaveGroupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return LeaveGroupRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseLeaveGroupRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetIconRequest() {
  return { chatGuid: "", data: new Uint8Array(0), clientMessageId: void 0 };
}
var SetIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.data.length !== 0) {
      writer.uint32(18).bytes(message.data);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      data: isSet4(object.data) ? bytesFromBase643(object.data) : new Uint8Array(0),
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes3(message.data);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.data = object.data ?? new Uint8Array(0);
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRemoveIconRequest() {
  return { chatGuid: "", clientMessageId: void 0 };
}
var RemoveIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRemoveIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      clientMessageId: isSet4(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet4(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RemoveIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRemoveIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetIconRequest() {
  return { chatGuid: "" };
}
var GetIconRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIconRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return GetIconRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIconRequest();
    message.chatGuid = object.chatGuid ?? "";
    return message;
  }
};
function createBaseGetIconResponse() {
  return { data: new Uint8Array(0), mimeType: "" };
}
var GetIconResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.mimeType !== "") {
      writer.uint32(18).string(message.mimeType);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetIconResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.data = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.mimeType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      data: isSet4(object.data) ? bytesFromBase643(object.data) : new Uint8Array(0),
      mimeType: isSet4(object.mimeType) ? globalThis.String(object.mimeType) : isSet4(object.mime_type) ? globalThis.String(object.mime_type) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes3(message.data);
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    return obj;
  },
  create(base) {
    return GetIconResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetIconResponse();
    message.data = object.data ?? new Uint8Array(0);
    message.mimeType = object.mimeType ?? "";
    return message;
  }
};
function createBaseSubscribeGroupEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeGroupEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet4(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet4(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeGroupEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeGroupEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeGroupEventsResponse() {
  return { sequence: void 0, groupChanged: void 0, heartbeat: void 0 };
}
var SubscribeGroupEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.groupChanged !== void 0) {
      GroupChangeEvent.encode(message.groupChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeGroupEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber2(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.groupChanged = GroupChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet4(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      groupChanged: isSet4(object.groupChanged) ? GroupChangeEvent.fromJSON(object.groupChanged) : isSet4(object.group_changed) ? GroupChangeEvent.fromJSON(object.group_changed) : void 0,
      heartbeat: isSet4(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.groupChanged !== void 0) {
      obj.groupChanged = GroupChangeEvent.toJSON(message.groupChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeGroupEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeGroupEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.groupChanged = object.groupChanged !== void 0 && object.groupChanged !== null ? GroupChangeEvent.fromPartial(object.groupChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var GroupServiceDefinition = {
  name: "GroupService",
  fullName: "photon.imessage.v1.GroupService",
  methods: {
    /** Writes */
    setDisplayName: {
      name: "SetDisplayName",
      requestType: SetDisplayNameRequest,
      requestStream: false,
      responseType: SetDisplayNameResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              30,
              58,
              1,
              42,
              34,
              25,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              115,
              101,
              116,
              68,
              105,
              115,
              112,
              108,
              97,
              121,
              78,
              97,
              109,
              101
            ])
          ]
        }
      }
    },
    addParticipants: {
      name: "AddParticipants",
      requestType: AddParticipantsRequest,
      requestStream: false,
      responseType: AddParticipantsResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              31,
              58,
              1,
              42,
              34,
              26,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              97,
              100,
              100,
              80,
              97,
              114,
              116,
              105,
              99,
              105,
              112,
              97,
              110,
              116,
              115
            ])
          ]
        }
      }
    },
    removeParticipants: {
      name: "RemoveParticipants",
      requestType: RemoveParticipantsRequest,
      requestStream: false,
      responseType: RemoveParticipantsResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              34,
              58,
              1,
              42,
              34,
              29,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              114,
              101,
              109,
              111,
              118,
              101,
              80,
              97,
              114,
              116,
              105,
              99,
              105,
              112,
              97,
              110,
              116,
              115
            ])
          ]
        }
      }
    },
    /**
     * Local user voluntarily leaves the group. The chat ceases to be
     * visible from this device, hence no Chat snapshot.
     */
    leaveGroup: {
      name: "LeaveGroup",
      requestType: LeaveGroupRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              21,
              58,
              1,
              42,
              34,
              16,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              108,
              101,
              97,
              118,
              101
            ])
          ]
        }
      }
    },
    /**
     * Over HTTP, `data` rides base64-encoded in the JSON body (proto3-JSON
     * `bytes` representation).
     */
    setIcon: {
      name: "SetIcon",
      requestType: SetIconRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              23,
              58,
              1,
              42,
              34,
              18,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              115,
              101,
              116,
              73,
              99,
              111,
              110
            ])
          ]
        }
      }
    },
    removeIcon: {
      name: "RemoveIcon",
      requestType: RemoveIconRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              26,
              58,
              1,
              42,
              34,
              21,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              114,
              101,
              109,
              111,
              118,
              101,
              73,
              99,
              111,
              110
            ])
          ]
        }
      }
    },
    /**
     * Reads. Over HTTP the icon bytes come back base64-encoded in JSON —
     * acceptable at group-icon sizes.
     */
    getIcon: {
      name: "GetIcon",
      requestType: GetIconRequest,
      requestStream: false,
      responseType: GetIconResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              17,
              18,
              15,
              47,
              118,
              49,
              47,
              103,
              114,
              111,
              117,
              112,
              115,
              58,
              105,
              99,
              111,
              110
            ])
          ]
        }
      }
    },
    /** Streams */
    subscribeGroupEvents: {
      name: "SubscribeGroupEvents",
      requestType: SubscribeGroupEventsRequest,
      requestStream: false,
      responseType: SubscribeGroupEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase643(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes3(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function longToNumber2(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet4(value) {
  return value !== null && value !== void 0;
}
function createBaseListSharedFriendLocationsRequest() {
  return {};
}
var ListSharedFriendLocationsRequest = {
  encode(_, writer = new BinaryWriter()) {
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListSharedFriendLocationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(_) {
    return {};
  },
  toJSON(_) {
    const obj = {};
    return obj;
  },
  create(base) {
    return ListSharedFriendLocationsRequest.fromPartial(base ?? {});
  },
  fromPartial(_) {
    const message = createBaseListSharedFriendLocationsRequest();
    return message;
  }
};
function createBaseListSharedFriendLocationsResponse() {
  return { locations: [] };
}
var ListSharedFriendLocationsResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.locations) {
      SharedFriendLocation.encode(v, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListSharedFriendLocationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.locations.push(SharedFriendLocation.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      locations: globalThis.Array.isArray(object?.locations) ? object.locations.map((e) => SharedFriendLocation.fromJSON(e)) : []
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.locations?.length) {
      obj.locations = message.locations.map((e) => SharedFriendLocation.toJSON(e));
    }
    return obj;
  },
  create(base) {
    return ListSharedFriendLocationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListSharedFriendLocationsResponse();
    message.locations = object.locations?.map((e) => SharedFriendLocation.fromPartial(e)) || [];
    return message;
  }
};
function createBaseGetSharedFriendLocationRequest() {
  return { address: "" };
}
var GetSharedFriendLocationRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSharedFriendLocationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet5(object.address) ? globalThis.String(object.address) : "" };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return GetSharedFriendLocationRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSharedFriendLocationRequest();
    message.address = object.address ?? "";
    return message;
  }
};
function createBaseGetSharedFriendLocationResponse() {
  return { location: void 0 };
}
var GetSharedFriendLocationResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.location !== void 0) {
      SharedFriendLocation.encode(message.location, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetSharedFriendLocationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.location = SharedFriendLocation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { location: isSet5(object.location) ? SharedFriendLocation.fromJSON(object.location) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.location !== void 0) {
      obj.location = SharedFriendLocation.toJSON(message.location);
    }
    return obj;
  },
  create(base) {
    return GetSharedFriendLocationResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetSharedFriendLocationResponse();
    message.location = object.location !== void 0 && object.location !== null ? SharedFriendLocation.fromPartial(object.location) : void 0;
    return message;
  }
};
function createBaseRequestFriendLocationSharingRequest() {
  return { chatGuid: "", address: "", clientMessageId: void 0 };
}
var RequestFriendLocationSharingRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestFriendLocationSharingRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet5(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet5(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      address: isSet5(object.address) ? globalThis.String(object.address) : "",
      clientMessageId: isSet5(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet5(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return RequestFriendLocationSharingRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestFriendLocationSharingRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.address = object.address ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseRequestFriendLocationSharingResponse() {
  return { address: "", status: "", reason: void 0, messageGuid: void 0 };
}
var RequestFriendLocationSharingResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.reason !== void 0) {
      writer.uint32(26).string(message.reason);
    }
    if (message.messageGuid !== void 0) {
      writer.uint32(34).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseRequestFriendLocationSharingResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      address: isSet5(object.address) ? globalThis.String(object.address) : "",
      status: isSet5(object.status) ? globalThis.String(object.status) : "",
      reason: isSet5(object.reason) ? globalThis.String(object.reason) : void 0,
      messageGuid: isSet5(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet5(object.message_guid) ? globalThis.String(object.message_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.reason !== void 0) {
      obj.reason = message.reason;
    }
    if (message.messageGuid !== void 0) {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return RequestFriendLocationSharingResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseRequestFriendLocationSharingResponse();
    message.address = object.address ?? "";
    message.status = object.status ?? "";
    message.reason = object.reason ?? void 0;
    message.messageGuid = object.messageGuid ?? void 0;
    return message;
  }
};
function createBaseWatchSharedFriendLocationsRequest() {
  return { address: void 0 };
}
var WatchSharedFriendLocationsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.address !== void 0) {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWatchSharedFriendLocationsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { address: isSet5(object.address) ? globalThis.String(object.address) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.address !== void 0) {
      obj.address = message.address;
    }
    return obj;
  },
  create(base) {
    return WatchSharedFriendLocationsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseWatchSharedFriendLocationsRequest();
    message.address = object.address ?? void 0;
    return message;
  }
};
function createBaseWatchSharedFriendLocationsResponse() {
  return { locationUpdated: void 0, heartbeat: void 0 };
}
var WatchSharedFriendLocationsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.locationUpdated !== void 0) {
      SharedFriendLocationUpdated.encode(message.locationUpdated, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseWatchSharedFriendLocationsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.locationUpdated = SharedFriendLocationUpdated.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      locationUpdated: isSet5(object.locationUpdated) ? SharedFriendLocationUpdated.fromJSON(object.locationUpdated) : isSet5(object.location_updated) ? SharedFriendLocationUpdated.fromJSON(object.location_updated) : void 0,
      heartbeat: isSet5(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.locationUpdated !== void 0) {
      obj.locationUpdated = SharedFriendLocationUpdated.toJSON(message.locationUpdated);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return WatchSharedFriendLocationsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseWatchSharedFriendLocationsResponse();
    message.locationUpdated = object.locationUpdated !== void 0 && object.locationUpdated !== null ? SharedFriendLocationUpdated.fromPartial(object.locationUpdated) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var LocationServiceDefinition = {
  name: "LocationService",
  fullName: "photon.imessage.v1.LocationService",
  methods: {
    listSharedFriendLocations: {
      name: "ListSharedFriendLocations",
      requestType: ListSharedFriendLocationsRequest,
      requestStream: false,
      responseType: ListSharedFriendLocationsResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              29,
              18,
              27,
              47,
              118,
              49,
              47,
              108,
              111,
              99,
              97,
              116,
              105,
              111,
              110,
              115,
              58,
              115,
              104,
              97,
              114,
              101,
              100,
              70,
              114,
              105,
              101,
              110,
              100,
              115
            ])
          ]
        }
      }
    },
    getSharedFriendLocation: {
      name: "GetSharedFriendLocation",
      requestType: GetSharedFriendLocationRequest,
      requestStream: false,
      responseType: GetSharedFriendLocationResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              28,
              18,
              26,
              47,
              118,
              49,
              47,
              108,
              111,
              99,
              97,
              116,
              105,
              111,
              110,
              115,
              58,
              115,
              104,
              97,
              114,
              101,
              100,
              70,
              114,
              105,
              101,
              110,
              100
            ])
          ]
        }
      }
    },
    requestFriendLocationSharing: {
      name: "RequestFriendLocationSharing",
      requestType: RequestFriendLocationSharingRequest,
      requestStream: false,
      responseType: RequestFriendLocationSharingResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              33,
              58,
              1,
              42,
              34,
              28,
              47,
              118,
              49,
              47,
              108,
              111,
              99,
              97,
              116,
              105,
              111,
              110,
              115,
              58,
              114,
              101,
              113,
              117,
              101,
              115,
              116,
              83,
              104,
              97,
              114,
              105,
              110,
              103
            ])
          ]
        }
      }
    },
    watchSharedFriendLocations: {
      name: "WatchSharedFriendLocations",
      requestType: WatchSharedFriendLocationsRequest,
      requestStream: false,
      responseType: WatchSharedFriendLocationsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function isSet5(value) {
  return value !== null && value !== void 0;
}
function createBaseMessageTarget() {
  return { chatGuid: "", messageGuid: "", targetPartIndex: void 0 };
}
var MessageTarget = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    if (message.targetPartIndex !== void 0) {
      writer.uint32(24).int32(message.targetPartIndex);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.targetPartIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet6(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet6(object.message_guid) ? globalThis.String(object.message_guid) : "",
      targetPartIndex: isSet6(object.targetPartIndex) ? globalThis.Number(object.targetPartIndex) : isSet6(object.target_part_index) ? globalThis.Number(object.target_part_index) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.targetPartIndex !== void 0) {
      obj.targetPartIndex = Math.round(message.targetPartIndex);
    }
    return obj;
  },
  create(base) {
    return MessageTarget.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageTarget();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    message.targetPartIndex = object.targetPartIndex ?? void 0;
    return message;
  }
};
function createBaseSendTextMessageRequest() {
  return {
    chatGuid: "",
    text: "",
    replyTo: void 0,
    subject: void 0,
    effectId: void 0,
    enableDataDetection: void 0,
    enableLinkPreview: void 0,
    formatting: [],
    clientMessageId: void 0
  };
}
var SendTextMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    if (message.effectId !== void 0) {
      writer.uint32(42).string(message.effectId);
    }
    if (message.enableDataDetection !== void 0) {
      writer.uint32(48).bool(message.enableDataDetection);
    }
    if (message.enableLinkPreview !== void 0) {
      writer.uint32(56).bool(message.enableLinkPreview);
    }
    for (const v of message.formatting) {
      TextFormat.encode(v, writer.uint32(66).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendTextMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.enableDataDetection = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.enableLinkPreview = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.formatting.push(TextFormat.decode(reader, reader.uint32()));
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      text: isSet6(object.text) ? globalThis.String(object.text) : "",
      replyTo: isSet6(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet6(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      subject: isSet6(object.subject) ? globalThis.String(object.subject) : void 0,
      effectId: isSet6(object.effectId) ? globalThis.String(object.effectId) : isSet6(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      enableDataDetection: isSet6(object.enableDataDetection) ? globalThis.Boolean(object.enableDataDetection) : isSet6(object.enable_data_detection) ? globalThis.Boolean(object.enable_data_detection) : void 0,
      enableLinkPreview: isSet6(object.enableLinkPreview) ? globalThis.Boolean(object.enableLinkPreview) : isSet6(object.enable_link_preview) ? globalThis.Boolean(object.enable_link_preview) : void 0,
      formatting: globalThis.Array.isArray(object?.formatting) ? object.formatting.map((e) => TextFormat.fromJSON(e)) : [],
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.enableDataDetection !== void 0) {
      obj.enableDataDetection = message.enableDataDetection;
    }
    if (message.enableLinkPreview !== void 0) {
      obj.enableLinkPreview = message.enableLinkPreview;
    }
    if (message.formatting?.length) {
      obj.formatting = message.formatting.map((e) => TextFormat.toJSON(e));
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendTextMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendTextMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.text = object.text ?? "";
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.subject = object.subject ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.enableDataDetection = object.enableDataDetection ?? void 0;
    message.enableLinkPreview = object.enableLinkPreview ?? void 0;
    message.formatting = object.formatting?.map((e) => TextFormat.fromPartial(e)) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSendAttachmentMessageRequest() {
  return {
    chatGuid: "",
    attachment: void 0,
    replyTo: void 0,
    effectId: void 0,
    isAudioMessage: void 0,
    clientMessageId: void 0
  };
}
var SendAttachmentMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.attachment !== void 0) {
      AttachmentRef.encode(message.attachment, writer.uint32(18).fork()).join();
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.effectId !== void 0) {
      writer.uint32(34).string(message.effectId);
    }
    if (message.isAudioMessage !== void 0) {
      writer.uint32(40).bool(message.isAudioMessage);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendAttachmentMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.attachment = AttachmentRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isAudioMessage = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      attachment: isSet6(object.attachment) ? AttachmentRef.fromJSON(object.attachment) : void 0,
      replyTo: isSet6(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet6(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      effectId: isSet6(object.effectId) ? globalThis.String(object.effectId) : isSet6(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      isAudioMessage: isSet6(object.isAudioMessage) ? globalThis.Boolean(object.isAudioMessage) : isSet6(object.is_audio_message) ? globalThis.Boolean(object.is_audio_message) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.attachment !== void 0) {
      obj.attachment = AttachmentRef.toJSON(message.attachment);
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.isAudioMessage !== void 0) {
      obj.isAudioMessage = message.isAudioMessage;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendAttachmentMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendAttachmentMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.attachment = object.attachment !== void 0 && object.attachment !== null ? AttachmentRef.fromPartial(object.attachment) : void 0;
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.effectId = object.effectId ?? void 0;
    message.isAudioMessage = object.isAudioMessage ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSendMultipartMessageRequest() {
  return {
    chatGuid: "",
    parts: [],
    replyTo: void 0,
    subject: void 0,
    effectId: void 0,
    enableDataDetection: void 0,
    clientMessageId: void 0
  };
}
var SendMultipartMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    for (const v of message.parts) {
      MessagePart.encode(v, writer.uint32(18).fork()).join();
    }
    if (message.replyTo !== void 0) {
      ReplyTarget.encode(message.replyTo, writer.uint32(26).fork()).join();
    }
    if (message.subject !== void 0) {
      writer.uint32(34).string(message.subject);
    }
    if (message.effectId !== void 0) {
      writer.uint32(42).string(message.effectId);
    }
    if (message.enableDataDetection !== void 0) {
      writer.uint32(48).bool(message.enableDataDetection);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendMultipartMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.parts.push(MessagePart.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.replyTo = ReplyTarget.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.subject = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.effectId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }
          message.enableDataDetection = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      parts: globalThis.Array.isArray(object?.parts) ? object.parts.map((e) => MessagePart.fromJSON(e)) : [],
      replyTo: isSet6(object.replyTo) ? ReplyTarget.fromJSON(object.replyTo) : isSet6(object.reply_to) ? ReplyTarget.fromJSON(object.reply_to) : void 0,
      subject: isSet6(object.subject) ? globalThis.String(object.subject) : void 0,
      effectId: isSet6(object.effectId) ? globalThis.String(object.effectId) : isSet6(object.effect_id) ? globalThis.String(object.effect_id) : void 0,
      enableDataDetection: isSet6(object.enableDataDetection) ? globalThis.Boolean(object.enableDataDetection) : isSet6(object.enable_data_detection) ? globalThis.Boolean(object.enable_data_detection) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.parts?.length) {
      obj.parts = message.parts.map((e) => MessagePart.toJSON(e));
    }
    if (message.replyTo !== void 0) {
      obj.replyTo = ReplyTarget.toJSON(message.replyTo);
    }
    if (message.subject !== void 0) {
      obj.subject = message.subject;
    }
    if (message.effectId !== void 0) {
      obj.effectId = message.effectId;
    }
    if (message.enableDataDetection !== void 0) {
      obj.enableDataDetection = message.enableDataDetection;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendMultipartMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendMultipartMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.parts = object.parts?.map((e) => MessagePart.fromPartial(e)) || [];
    message.replyTo = object.replyTo !== void 0 && object.replyTo !== null ? ReplyTarget.fromPartial(object.replyTo) : void 0;
    message.subject = object.subject ?? void 0;
    message.effectId = object.effectId ?? void 0;
    message.enableDataDetection = object.enableDataDetection ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseMiniAppLayout() {
  return {
    caption: void 0,
    subcaption: void 0,
    trailingCaption: void 0,
    trailingSubcaption: void 0,
    image: void 0,
    imageTitle: void 0,
    imageSubtitle: void 0,
    summary: void 0
  };
}
var MiniAppLayout = {
  encode(message, writer = new BinaryWriter()) {
    if (message.caption !== void 0) {
      writer.uint32(42).string(message.caption);
    }
    if (message.subcaption !== void 0) {
      writer.uint32(26).string(message.subcaption);
    }
    if (message.trailingCaption !== void 0) {
      writer.uint32(50).string(message.trailingCaption);
    }
    if (message.trailingSubcaption !== void 0) {
      writer.uint32(58).string(message.trailingSubcaption);
    }
    if (message.image !== void 0) {
      writer.uint32(34).bytes(message.image);
    }
    if (message.imageTitle !== void 0) {
      writer.uint32(10).string(message.imageTitle);
    }
    if (message.imageSubtitle !== void 0) {
      writer.uint32(18).string(message.imageSubtitle);
    }
    if (message.summary !== void 0) {
      writer.uint32(66).string(message.summary);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMiniAppLayout();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.caption = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.subcaption = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.trailingCaption = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.trailingSubcaption = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.image = reader.bytes();
          continue;
        }
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.imageTitle = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.imageSubtitle = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }
          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      caption: isSet6(object.caption) ? globalThis.String(object.caption) : void 0,
      subcaption: isSet6(object.subcaption) ? globalThis.String(object.subcaption) : void 0,
      trailingCaption: isSet6(object.trailingCaption) ? globalThis.String(object.trailingCaption) : isSet6(object.trailing_caption) ? globalThis.String(object.trailing_caption) : void 0,
      trailingSubcaption: isSet6(object.trailingSubcaption) ? globalThis.String(object.trailingSubcaption) : isSet6(object.trailing_subcaption) ? globalThis.String(object.trailing_subcaption) : void 0,
      image: isSet6(object.image) ? bytesFromBase644(object.image) : void 0,
      imageTitle: isSet6(object.imageTitle) ? globalThis.String(object.imageTitle) : isSet6(object.image_title) ? globalThis.String(object.image_title) : void 0,
      imageSubtitle: isSet6(object.imageSubtitle) ? globalThis.String(object.imageSubtitle) : isSet6(object.image_subtitle) ? globalThis.String(object.image_subtitle) : void 0,
      summary: isSet6(object.summary) ? globalThis.String(object.summary) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.caption !== void 0) {
      obj.caption = message.caption;
    }
    if (message.subcaption !== void 0) {
      obj.subcaption = message.subcaption;
    }
    if (message.trailingCaption !== void 0) {
      obj.trailingCaption = message.trailingCaption;
    }
    if (message.trailingSubcaption !== void 0) {
      obj.trailingSubcaption = message.trailingSubcaption;
    }
    if (message.image !== void 0) {
      obj.image = base64FromBytes4(message.image);
    }
    if (message.imageTitle !== void 0) {
      obj.imageTitle = message.imageTitle;
    }
    if (message.imageSubtitle !== void 0) {
      obj.imageSubtitle = message.imageSubtitle;
    }
    if (message.summary !== void 0) {
      obj.summary = message.summary;
    }
    return obj;
  },
  create(base) {
    return MiniAppLayout.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMiniAppLayout();
    message.caption = object.caption ?? void 0;
    message.subcaption = object.subcaption ?? void 0;
    message.trailingCaption = object.trailingCaption ?? void 0;
    message.trailingSubcaption = object.trailingSubcaption ?? void 0;
    message.image = object.image ?? void 0;
    message.imageTitle = object.imageTitle ?? void 0;
    message.imageSubtitle = object.imageSubtitle ?? void 0;
    message.summary = object.summary ?? void 0;
    return message;
  }
};
function createBaseSendCustomizedMiniAppMessageRequest() {
  return {
    chatGuid: "",
    teamId: "",
    extensionBundleId: "",
    appName: "",
    url: "",
    layout: void 0,
    appStoreId: void 0,
    live: void 0,
    clientMessageId: void 0
  };
}
var SendCustomizedMiniAppMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.teamId !== "") {
      writer.uint32(18).string(message.teamId);
    }
    if (message.extensionBundleId !== "") {
      writer.uint32(26).string(message.extensionBundleId);
    }
    if (message.appName !== "") {
      writer.uint32(34).string(message.appName);
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    if (message.layout !== void 0) {
      MiniAppLayout.encode(message.layout, writer.uint32(50).fork()).join();
    }
    if (message.appStoreId !== void 0) {
      writer.uint32(56).int64(message.appStoreId);
    }
    if (message.live !== void 0) {
      writer.uint32(64).bool(message.live);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSendCustomizedMiniAppMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.teamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.extensionBundleId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.appName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.layout = MiniAppLayout.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.appStoreId = longToNumber3(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }
          message.live = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      teamId: isSet6(object.teamId) ? globalThis.String(object.teamId) : isSet6(object.team_id) ? globalThis.String(object.team_id) : "",
      extensionBundleId: isSet6(object.extensionBundleId) ? globalThis.String(object.extensionBundleId) : isSet6(object.extension_bundle_id) ? globalThis.String(object.extension_bundle_id) : "",
      appName: isSet6(object.appName) ? globalThis.String(object.appName) : isSet6(object.app_name) ? globalThis.String(object.app_name) : "",
      url: isSet6(object.url) ? globalThis.String(object.url) : "",
      layout: isSet6(object.layout) ? MiniAppLayout.fromJSON(object.layout) : void 0,
      appStoreId: isSet6(object.appStoreId) ? globalThis.Number(object.appStoreId) : isSet6(object.app_store_id) ? globalThis.Number(object.app_store_id) : void 0,
      live: isSet6(object.live) ? globalThis.Boolean(object.live) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.teamId !== "") {
      obj.teamId = message.teamId;
    }
    if (message.extensionBundleId !== "") {
      obj.extensionBundleId = message.extensionBundleId;
    }
    if (message.appName !== "") {
      obj.appName = message.appName;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.layout !== void 0) {
      obj.layout = MiniAppLayout.toJSON(message.layout);
    }
    if (message.appStoreId !== void 0) {
      obj.appStoreId = Math.round(message.appStoreId);
    }
    if (message.live !== void 0) {
      obj.live = message.live;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SendCustomizedMiniAppMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSendCustomizedMiniAppMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.teamId = object.teamId ?? "";
    message.extensionBundleId = object.extensionBundleId ?? "";
    message.appName = object.appName ?? "";
    message.url = object.url ?? "";
    message.layout = object.layout !== void 0 && object.layout !== null ? MiniAppLayout.fromPartial(object.layout) : void 0;
    message.appStoreId = object.appStoreId ?? void 0;
    message.live = object.live ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseMiniAppCardSession() {
  return { messageGuid: "", chatGuid: "", sessionId: "", targetMessageGuid: "" };
}
var MiniAppCardSession = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(10).string(message.messageGuid);
    }
    if (message.chatGuid !== "") {
      writer.uint32(18).string(message.chatGuid);
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    if (message.targetMessageGuid !== "") {
      writer.uint32(34).string(message.targetMessageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMiniAppCardSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.sessionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.targetMessageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet6(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet6(object.message_guid) ? globalThis.String(object.message_guid) : "",
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      sessionId: isSet6(object.sessionId) ? globalThis.String(object.sessionId) : isSet6(object.session_id) ? globalThis.String(object.session_id) : "",
      targetMessageGuid: isSet6(object.targetMessageGuid) ? globalThis.String(object.targetMessageGuid) : isSet6(object.target_message_guid) ? globalThis.String(object.target_message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.targetMessageGuid !== "") {
      obj.targetMessageGuid = message.targetMessageGuid;
    }
    return obj;
  },
  create(base) {
    return MiniAppCardSession.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMiniAppCardSession();
    message.messageGuid = object.messageGuid ?? "";
    message.chatGuid = object.chatGuid ?? "";
    message.sessionId = object.sessionId ?? "";
    message.targetMessageGuid = object.targetMessageGuid ?? "";
    return message;
  }
};
function createBaseUpdateCustomizedMiniAppMessageRequest() {
  return {
    session: void 0,
    teamId: "",
    extensionBundleId: "",
    appName: "",
    url: "",
    layout: void 0,
    appStoreId: void 0,
    live: void 0,
    clientMessageId: void 0
  };
}
var UpdateCustomizedMiniAppMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.session !== void 0) {
      MiniAppCardSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.teamId !== "") {
      writer.uint32(18).string(message.teamId);
    }
    if (message.extensionBundleId !== "") {
      writer.uint32(26).string(message.extensionBundleId);
    }
    if (message.appName !== "") {
      writer.uint32(34).string(message.appName);
    }
    if (message.url !== "") {
      writer.uint32(42).string(message.url);
    }
    if (message.layout !== void 0) {
      MiniAppLayout.encode(message.layout, writer.uint32(50).fork()).join();
    }
    if (message.appStoreId !== void 0) {
      writer.uint32(56).int64(message.appStoreId);
    }
    if (message.live !== void 0) {
      writer.uint32(64).bool(message.live);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUpdateCustomizedMiniAppMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.session = MiniAppCardSession.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.teamId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.extensionBundleId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }
          message.appName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.url = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.layout = MiniAppLayout.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }
          message.appStoreId = longToNumber3(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }
          message.live = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      session: isSet6(object.session) ? MiniAppCardSession.fromJSON(object.session) : void 0,
      teamId: isSet6(object.teamId) ? globalThis.String(object.teamId) : isSet6(object.team_id) ? globalThis.String(object.team_id) : "",
      extensionBundleId: isSet6(object.extensionBundleId) ? globalThis.String(object.extensionBundleId) : isSet6(object.extension_bundle_id) ? globalThis.String(object.extension_bundle_id) : "",
      appName: isSet6(object.appName) ? globalThis.String(object.appName) : isSet6(object.app_name) ? globalThis.String(object.app_name) : "",
      url: isSet6(object.url) ? globalThis.String(object.url) : "",
      layout: isSet6(object.layout) ? MiniAppLayout.fromJSON(object.layout) : void 0,
      appStoreId: isSet6(object.appStoreId) ? globalThis.Number(object.appStoreId) : isSet6(object.app_store_id) ? globalThis.Number(object.app_store_id) : void 0,
      live: isSet6(object.live) ? globalThis.Boolean(object.live) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.session !== void 0) {
      obj.session = MiniAppCardSession.toJSON(message.session);
    }
    if (message.teamId !== "") {
      obj.teamId = message.teamId;
    }
    if (message.extensionBundleId !== "") {
      obj.extensionBundleId = message.extensionBundleId;
    }
    if (message.appName !== "") {
      obj.appName = message.appName;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.layout !== void 0) {
      obj.layout = MiniAppLayout.toJSON(message.layout);
    }
    if (message.appStoreId !== void 0) {
      obj.appStoreId = Math.round(message.appStoreId);
    }
    if (message.live !== void 0) {
      obj.live = message.live;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return UpdateCustomizedMiniAppMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUpdateCustomizedMiniAppMessageRequest();
    message.session = object.session !== void 0 && object.session !== null ? MiniAppCardSession.fromPartial(object.session) : void 0;
    message.teamId = object.teamId ?? "";
    message.extensionBundleId = object.extensionBundleId ?? "";
    message.appName = object.appName ?? "";
    message.url = object.url ?? "";
    message.layout = object.layout !== void 0 && object.layout !== null ? MiniAppLayout.fromPartial(object.layout) : void 0;
    message.appStoreId = object.appStoreId ?? void 0;
    message.live = object.live ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseMessageResponse() {
  return { message: void 0, miniAppCardSession: void 0 };
}
var MessageResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.message !== void 0) {
      Message.encode(message.message, writer.uint32(10).fork()).join();
    }
    if (message.miniAppCardSession !== void 0) {
      MiniAppCardSession.encode(message.miniAppCardSession, writer.uint32(18).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.miniAppCardSession = MiniAppCardSession.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      message: isSet6(object.message) ? Message.fromJSON(object.message) : void 0,
      miniAppCardSession: isSet6(object.miniAppCardSession) ? MiniAppCardSession.fromJSON(object.miniAppCardSession) : isSet6(object.mini_app_card_session) ? MiniAppCardSession.fromJSON(object.mini_app_card_session) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.message !== void 0) {
      obj.message = Message.toJSON(message.message);
    }
    if (message.miniAppCardSession !== void 0) {
      obj.miniAppCardSession = MiniAppCardSession.toJSON(message.miniAppCardSession);
    }
    return obj;
  },
  create(base) {
    return MessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseMessageResponse();
    message.message = object.message !== void 0 && object.message !== null ? Message.fromPartial(object.message) : void 0;
    message.miniAppCardSession = object.miniAppCardSession !== void 0 && object.miniAppCardSession !== null ? MiniAppCardSession.fromPartial(object.miniAppCardSession) : void 0;
    return message;
  }
};
function createBaseEditMessageRequest() {
  return { target: void 0, newText: "", backwardCompatText: void 0, clientMessageId: void 0 };
}
var EditMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.newText !== "") {
      writer.uint32(18).string(message.newText);
    }
    if (message.backwardCompatText !== void 0) {
      writer.uint32(26).string(message.backwardCompatText);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseEditMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.newText = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.backwardCompatText = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet6(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      newText: isSet6(object.newText) ? globalThis.String(object.newText) : isSet6(object.new_text) ? globalThis.String(object.new_text) : "",
      backwardCompatText: isSet6(object.backwardCompatText) ? globalThis.String(object.backwardCompatText) : isSet6(object.backward_compat_text) ? globalThis.String(object.backward_compat_text) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.newText !== "") {
      obj.newText = message.newText;
    }
    if (message.backwardCompatText !== void 0) {
      obj.backwardCompatText = message.backwardCompatText;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return EditMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseEditMessageRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.newText = object.newText ?? "";
    message.backwardCompatText = object.backwardCompatText ?? void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseUnsendMessageRequest() {
  return { target: void 0, clientMessageId: void 0 };
}
var UnsendMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnsendMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet6(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return UnsendMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUnsendMessageRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseSetReactionRequest() {
  return { target: void 0, reaction: void 0, isSet: false, clientMessageId: void 0 };
}
var SetReactionRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.reaction !== void 0) {
      MessageReaction.encode(message.reaction, writer.uint32(18).fork()).join();
    }
    if (message.isSet !== false) {
      writer.uint32(24).bool(message.isSet);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSetReactionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.reaction = MessageReaction.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.isSet = reader.bool();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet6(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      reaction: isSet6(object.reaction) ? MessageReaction.fromJSON(object.reaction) : void 0,
      isSet: isSet6(object.isSet) ? globalThis.Boolean(object.isSet) : isSet6(object.is_set) ? globalThis.Boolean(object.is_set) : false,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.reaction !== void 0) {
      obj.reaction = MessageReaction.toJSON(message.reaction);
    }
    if (message.isSet !== false) {
      obj.isSet = message.isSet;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return SetReactionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSetReactionRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.reaction = object.reaction !== void 0 && object.reaction !== null ? MessageReaction.fromPartial(object.reaction) : void 0;
    message.isSet = object.isSet ?? false;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBasePlaceStickerRequest() {
  return { target: void 0, sticker: void 0, placement: void 0, clientMessageId: void 0 };
}
var PlaceStickerRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.target !== void 0) {
      MessageTarget.encode(message.target, writer.uint32(10).fork()).join();
    }
    if (message.sticker !== void 0) {
      AttachmentRef.encode(message.sticker, writer.uint32(18).fork()).join();
    }
    if (message.placement !== void 0) {
      StickerPlacement.encode(message.placement, writer.uint32(26).fork()).join();
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePlaceStickerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.target = MessageTarget.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.sticker = AttachmentRef.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.placement = StickerPlacement.decode(reader, reader.uint32());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      target: isSet6(object.target) ? MessageTarget.fromJSON(object.target) : void 0,
      sticker: isSet6(object.sticker) ? AttachmentRef.fromJSON(object.sticker) : void 0,
      placement: isSet6(object.placement) ? StickerPlacement.fromJSON(object.placement) : void 0,
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.target !== void 0) {
      obj.target = MessageTarget.toJSON(message.target);
    }
    if (message.sticker !== void 0) {
      obj.sticker = AttachmentRef.toJSON(message.sticker);
    }
    if (message.placement !== void 0) {
      obj.placement = StickerPlacement.toJSON(message.placement);
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return PlaceStickerRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePlaceStickerRequest();
    message.target = object.target !== void 0 && object.target !== null ? MessageTarget.fromPartial(object.target) : void 0;
    message.sticker = object.sticker !== void 0 && object.sticker !== null ? AttachmentRef.fromPartial(object.sticker) : void 0;
    message.placement = object.placement !== void 0 && object.placement !== null ? StickerPlacement.fromPartial(object.placement) : void 0;
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseNotifySilencedMessageRequest() {
  return { chatGuid: "", messageGuid: "", clientMessageId: void 0 };
}
var NotifySilencedMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseNotifySilencedMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet6(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet6(object.message_guid) ? globalThis.String(object.message_guid) : "",
      clientMessageId: isSet6(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet6(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return NotifySilencedMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseNotifySilencedMessageRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetMessageRequest() {
  return { messageGuid: "" };
}
var GetMessageRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messageGuid: isSet6(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet6(object.message_guid) ? globalThis.String(object.message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return GetMessageRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetMessageRequest();
    message.messageGuid = object.messageGuid ?? "";
    return message;
  }
};
function createBaseGetMessageResponse() {
  return { message: void 0 };
}
var GetMessageResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.message !== void 0) {
      Message.encode(message.message, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.message = Message.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { message: isSet6(object.message) ? Message.fromJSON(object.message) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.message !== void 0) {
      obj.message = Message.toJSON(message.message);
    }
    return obj;
  },
  create(base) {
    return GetMessageResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetMessageResponse();
    message.message = object.message !== void 0 && object.message !== null ? Message.fromPartial(object.message) : void 0;
    return message;
  }
};
function createBaseListRecentMessagesRequest() {
  return {
    pageSize: void 0,
    pageToken: void 0,
    isFromMe: void 0,
    isRead: void 0,
    before: void 0,
    after: void 0
  };
}
var ListRecentMessagesRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pageSize !== void 0) {
      writer.uint32(8).int32(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      writer.uint32(18).string(message.pageToken);
    }
    if (message.isFromMe !== void 0) {
      writer.uint32(24).bool(message.isFromMe);
    }
    if (message.isRead !== void 0) {
      writer.uint32(32).bool(message.isRead);
    }
    if (message.before !== void 0) {
      Timestamp.encode(toTimestamp(message.before), writer.uint32(42).fork()).join();
    }
    if (message.after !== void 0) {
      Timestamp.encode(toTimestamp(message.after), writer.uint32(50).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListRecentMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.pageSize = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.pageToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isRead = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }
          message.before = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.after = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pageSize: isSet6(object.pageSize) ? globalThis.Number(object.pageSize) : isSet6(object.page_size) ? globalThis.Number(object.page_size) : void 0,
      pageToken: isSet6(object.pageToken) ? globalThis.String(object.pageToken) : isSet6(object.page_token) ? globalThis.String(object.page_token) : void 0,
      isFromMe: isSet6(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet6(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : void 0,
      isRead: isSet6(object.isRead) ? globalThis.Boolean(object.isRead) : isSet6(object.is_read) ? globalThis.Boolean(object.is_read) : void 0,
      before: isSet6(object.before) ? fromJsonTimestamp(object.before) : void 0,
      after: isSet6(object.after) ? fromJsonTimestamp(object.after) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pageSize !== void 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      obj.pageToken = message.pageToken;
    }
    if (message.isFromMe !== void 0) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.isRead !== void 0) {
      obj.isRead = message.isRead;
    }
    if (message.before !== void 0) {
      obj.before = message.before.toISOString();
    }
    if (message.after !== void 0) {
      obj.after = message.after.toISOString();
    }
    return obj;
  },
  create(base) {
    return ListRecentMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListRecentMessagesRequest();
    message.pageSize = object.pageSize ?? void 0;
    message.pageToken = object.pageToken ?? void 0;
    message.isFromMe = object.isFromMe ?? void 0;
    message.isRead = object.isRead ?? void 0;
    message.before = object.before ?? void 0;
    message.after = object.after ?? void 0;
    return message;
  }
};
function createBaseListRecentMessagesResponse() {
  return { messages: [], nextPageToken: void 0 };
}
var ListRecentMessagesResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.messages) {
      Message.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== void 0) {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListRecentMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messages.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: globalThis.Array.isArray(object?.messages) ? object.messages.map((e) => Message.fromJSON(e)) : [],
      nextPageToken: isSet6(object.nextPageToken) ? globalThis.String(object.nextPageToken) : isSet6(object.next_page_token) ? globalThis.String(object.next_page_token) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => Message.toJSON(e));
    }
    if (message.nextPageToken !== void 0) {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },
  create(base) {
    return ListRecentMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListRecentMessagesResponse();
    message.messages = object.messages?.map((e) => Message.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? void 0;
    return message;
  }
};
function createBaseListChatMessagesRequest() {
  return {
    chatGuid: "",
    pageSize: void 0,
    pageToken: void 0,
    isFromMe: void 0,
    isRead: void 0,
    before: void 0,
    after: void 0
  };
}
var ListChatMessagesRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.pageSize !== void 0) {
      writer.uint32(16).int32(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      writer.uint32(26).string(message.pageToken);
    }
    if (message.isFromMe !== void 0) {
      writer.uint32(32).bool(message.isFromMe);
    }
    if (message.isRead !== void 0) {
      writer.uint32(40).bool(message.isRead);
    }
    if (message.before !== void 0) {
      Timestamp.encode(toTimestamp(message.before), writer.uint32(50).fork()).join();
    }
    if (message.after !== void 0) {
      Timestamp.encode(toTimestamp(message.after), writer.uint32(58).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListChatMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }
          message.pageSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.pageToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }
          message.isFromMe = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }
          message.isRead = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }
          message.before = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }
          message.after = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      pageSize: isSet6(object.pageSize) ? globalThis.Number(object.pageSize) : isSet6(object.page_size) ? globalThis.Number(object.page_size) : void 0,
      pageToken: isSet6(object.pageToken) ? globalThis.String(object.pageToken) : isSet6(object.page_token) ? globalThis.String(object.page_token) : void 0,
      isFromMe: isSet6(object.isFromMe) ? globalThis.Boolean(object.isFromMe) : isSet6(object.is_from_me) ? globalThis.Boolean(object.is_from_me) : void 0,
      isRead: isSet6(object.isRead) ? globalThis.Boolean(object.isRead) : isSet6(object.is_read) ? globalThis.Boolean(object.is_read) : void 0,
      before: isSet6(object.before) ? fromJsonTimestamp(object.before) : void 0,
      after: isSet6(object.after) ? fromJsonTimestamp(object.after) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.pageSize !== void 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageToken !== void 0) {
      obj.pageToken = message.pageToken;
    }
    if (message.isFromMe !== void 0) {
      obj.isFromMe = message.isFromMe;
    }
    if (message.isRead !== void 0) {
      obj.isRead = message.isRead;
    }
    if (message.before !== void 0) {
      obj.before = message.before.toISOString();
    }
    if (message.after !== void 0) {
      obj.after = message.after.toISOString();
    }
    return obj;
  },
  create(base) {
    return ListChatMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListChatMessagesRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.pageSize = object.pageSize ?? void 0;
    message.pageToken = object.pageToken ?? void 0;
    message.isFromMe = object.isFromMe ?? void 0;
    message.isRead = object.isRead ?? void 0;
    message.before = object.before ?? void 0;
    message.after = object.after ?? void 0;
    return message;
  }
};
function createBaseListChatMessagesResponse() {
  return { messages: [], nextPageToken: void 0 };
}
var ListChatMessagesResponse = {
  encode(message, writer = new BinaryWriter()) {
    for (const v of message.messages) {
      Message.encode(v, writer.uint32(10).fork()).join();
    }
    if (message.nextPageToken !== void 0) {
      writer.uint32(18).string(message.nextPageToken);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseListChatMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.messages.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.nextPageToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      messages: globalThis.Array.isArray(object?.messages) ? object.messages.map((e) => Message.fromJSON(e)) : [],
      nextPageToken: isSet6(object.nextPageToken) ? globalThis.String(object.nextPageToken) : isSet6(object.next_page_token) ? globalThis.String(object.next_page_token) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => Message.toJSON(e));
    }
    if (message.nextPageToken !== void 0) {
      obj.nextPageToken = message.nextPageToken;
    }
    return obj;
  },
  create(base) {
    return ListChatMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseListChatMessagesResponse();
    message.messages = object.messages?.map((e) => Message.fromPartial(e)) || [];
    message.nextPageToken = object.nextPageToken ?? void 0;
    return message;
  }
};
function createBaseGetEmbeddedMediaRequest() {
  return { chatGuid: "", messageGuid: "" };
}
var GetEmbeddedMediaRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.messageGuid !== "") {
      writer.uint32(18).string(message.messageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetEmbeddedMediaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.messageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      messageGuid: isSet6(object.messageGuid) ? globalThis.String(object.messageGuid) : isSet6(object.message_guid) ? globalThis.String(object.message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.messageGuid !== "") {
      obj.messageGuid = message.messageGuid;
    }
    return obj;
  },
  create(base) {
    return GetEmbeddedMediaRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetEmbeddedMediaRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.messageGuid = object.messageGuid ?? "";
    return message;
  }
};
function createBaseGetEmbeddedMediaResponse() {
  return { media: void 0 };
}
var GetEmbeddedMediaResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.media !== void 0) {
      EmbeddedMedia.encode(message.media, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetEmbeddedMediaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.media = EmbeddedMedia.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { media: isSet6(object.media) ? EmbeddedMedia.fromJSON(object.media) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.media !== void 0) {
      obj.media = EmbeddedMedia.toJSON(message.media);
    }
    return obj;
  },
  create(base) {
    return GetEmbeddedMediaResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetEmbeddedMediaResponse();
    message.media = object.media !== void 0 && object.media !== null ? EmbeddedMedia.fromPartial(object.media) : void 0;
    return message;
  }
};
function createBaseSubscribeMessageEventsRequest() {
  return { chatGuid: void 0 };
}
var SubscribeMessageEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== void 0) {
      writer.uint32(10).string(message.chatGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeMessageEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet6(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet6(object.chat_guid) ? globalThis.String(object.chat_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== void 0) {
      obj.chatGuid = message.chatGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribeMessageEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeMessageEventsRequest();
    message.chatGuid = object.chatGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribeMessageEventsResponse() {
  return { sequence: void 0, messageChanged: void 0, heartbeat: void 0 };
}
var SubscribeMessageEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      MessageChangeEvent.encode(message.messageChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribeMessageEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber3(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.messageChanged = MessageChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet6(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      messageChanged: isSet6(object.messageChanged) ? MessageChangeEvent.fromJSON(object.messageChanged) : isSet6(object.message_changed) ? MessageChangeEvent.fromJSON(object.message_changed) : void 0,
      heartbeat: isSet6(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.messageChanged !== void 0) {
      obj.messageChanged = MessageChangeEvent.toJSON(message.messageChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribeMessageEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribeMessageEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.messageChanged = object.messageChanged !== void 0 && object.messageChanged !== null ? MessageChangeEvent.fromPartial(object.messageChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var MessageServiceDefinition = {
  name: "MessageService",
  fullName: "photon.imessage.v1.MessageService",
  methods: {
    /**
     * Writes
     *
     * HTTP mapping convention (governs all annotations in this package):
     * - `chat_guid` values (e.g. `iMessage;-;+15551234567`) are hostile to URL
     *   paths and never appear in one — they ride in the request body, or in
     *   query parameters for reads.
     * - `message_guid` values (UUID-shaped, or proxy-virtual `spc-*`) are
     *   path-safe and map to resource paths.
     * - Non-CRUD mutations use the `:customVerb` suffix style (AIP-136).
     */
    sendTextMessage: {
      name: "SendTextMessage",
      requestType: SendTextMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              26,
              58,
              1,
              42,
              34,
              21,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              115,
              101,
              110,
              100,
              84,
              101,
              120,
              116
            ])
          ]
        }
      }
    },
    sendAttachmentMessage: {
      name: "SendAttachmentMessage",
      requestType: SendAttachmentMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              32,
              58,
              1,
              42,
              34,
              27,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              115,
              101,
              110,
              100,
              65,
              116,
              116,
              97,
              99,
              104,
              109,
              101,
              110,
              116
            ])
          ]
        }
      }
    },
    sendMultipartMessage: {
      name: "SendMultipartMessage",
      requestType: SendMultipartMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              31,
              58,
              1,
              42,
              34,
              26,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              115,
              101,
              110,
              100,
              77,
              117,
              108,
              116,
              105,
              112,
              97,
              114,
              116
            ])
          ]
        }
      }
    },
    /** Sends an iMessage mini-app card backed by the caller's own extension. */
    sendCustomizedMiniAppMessage: {
      name: "SendCustomizedMiniAppMessage",
      requestType: SendCustomizedMiniAppMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              39,
              58,
              1,
              42,
              34,
              34,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              115,
              101,
              110,
              100,
              67,
              117,
              115,
              116,
              111,
              109,
              105,
              122,
              101,
              100,
              77,
              105,
              110,
              105,
              65,
              112,
              112
            ])
          ]
        }
      }
    },
    /** Updates an iMessage mini-app card backed by the caller's own extension. */
    updateCustomizedMiniAppMessage: {
      name: "UpdateCustomizedMiniAppMessage",
      requestType: UpdateCustomizedMiniAppMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              41,
              58,
              1,
              42,
              34,
              36,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              117,
              112,
              100,
              97,
              116,
              101,
              67,
              117,
              115,
              116,
              111,
              109,
              105,
              122,
              101,
              100,
              77,
              105,
              110,
              105,
              65,
              112,
              112
            ])
          ]
        }
      }
    },
    editMessage: {
      name: "EditMessage",
      requestType: EditMessageRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              22,
              58,
              1,
              42,
              34,
              17,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              101,
              100,
              105,
              116
            ])
          ]
        }
      }
    },
    /** Retracts an existing message and returns Empty after helper success. */
    unsendMessage: {
      name: "UnsendMessage",
      requestType: UnsendMessageRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              24,
              58,
              1,
              42,
              34,
              19,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              117,
              110,
              115,
              101,
              110,
              100
            ])
          ]
        }
      }
    },
    setReaction: {
      name: "SetReaction",
      requestType: SetReactionRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              29,
              58,
              1,
              42,
              34,
              24,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              115,
              101,
              116,
              82,
              101,
              97,
              99,
              116,
              105,
              111,
              110
            ])
          ]
        }
      }
    },
    placeSticker: {
      name: "PlaceSticker",
      requestType: PlaceStickerRequest,
      requestStream: false,
      responseType: MessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              30,
              58,
              1,
              42,
              34,
              25,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              112,
              108,
              97,
              99,
              101,
              83,
              116,
              105,
              99,
              107,
              101,
              114
            ])
          ]
        }
      }
    },
    /** Triggers Apple's per-message "Notify Anyway" action. */
    notifySilencedMessage: {
      name: "NotifySilencedMessage",
      requestType: NotifySilencedMessageRequest,
      requestStream: false,
      responseType: Empty,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              32,
              58,
              1,
              42,
              34,
              27,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              110,
              111,
              116,
              105,
              102,
              121,
              83,
              105,
              108,
              101,
              110,
              99,
              101,
              100
            ])
          ]
        }
      }
    },
    /** Reads */
    getMessage: {
      name: "GetMessage",
      requestType: GetMessageRequest,
      requestStream: false,
      responseType: GetMessageResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              29,
              18,
              27,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              47,
              123,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              95,
              103,
              117,
              105,
              100,
              125
            ])
          ]
        }
      }
    },
    /**
     * Query parameters over HTTP: `pageSize` (int), `isFromMe`/`isRead`
     * (true|false), `before`/`after` (RFC 3339 timestamps, the proto3-JSON
     * form of `google.protobuf.Timestamp`).
     */
    listRecentMessages: {
      name: "ListRecentMessages",
      requestType: ListRecentMessagesRequest,
      requestStream: false,
      responseType: ListRecentMessagesResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              25,
              18,
              23,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              108,
              105,
              115,
              116,
              82,
              101,
              99,
              101,
              110,
              116
            ])
          ]
        }
      }
    },
    /** Same query-parameter shapes as `ListRecentMessages`, plus `chatGuid`. */
    listChatMessages: {
      name: "ListChatMessages",
      requestType: ListChatMessagesRequest,
      requestStream: false,
      responseType: ListChatMessagesResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              25,
              18,
              23,
              47,
              118,
              49,
              47,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              115,
              58,
              108,
              105,
              115,
              116,
              66,
              121,
              67,
              104,
              97,
              116
            ])
          ]
        }
      }
    },
    /**
     * No HTTP mapping on purpose: the response carries raw media bytes
     * (`EmbeddedMedia.data`), which don't belong base64-encoded in JSON.
     * Served over HTTP by a dedicated raw-bytes route in the transcoding
     * middleware alongside Upload/DownloadAttachment.
     */
    getEmbeddedMedia: {
      name: "GetEmbeddedMedia",
      requestType: GetEmbeddedMediaRequest,
      requestStream: false,
      responseType: GetEmbeddedMediaResponse,
      responseStream: false,
      options: {}
    },
    /**
     * Live durable-event subscription. Pair with `EventService.CatchUpEvents`
     * for gap-free history-then-live consumption.
     */
    subscribeMessageEvents: {
      name: "SubscribeMessageEvents",
      requestType: SubscribeMessageEventsRequest,
      requestStream: false,
      responseType: SubscribeMessageEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function bytesFromBase644(b64) {
  if (globalThis.Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}
function base64FromBytes4(arr) {
  if (globalThis.Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}
function toTimestamp(date) {
  const seconds = Math.trunc(date.getTime() / 1e3);
  const nanos = date.getTime() % 1e3 * 1e6;
  return { seconds, nanos };
}
function fromTimestamp(t) {
  let millis = (t.seconds || 0) * 1e3;
  millis += (t.nanos || 0) / 1e6;
  return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}
function longToNumber3(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet6(value) {
  return value !== null && value !== void 0;
}
function createBaseCreatePollRequest() {
  return { chatGuid: "", title: "", options: [], clientMessageId: void 0 };
}
var CreatePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.chatGuid !== "") {
      writer.uint32(10).string(message.chatGuid);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    for (const v of message.options) {
      writer.uint32(26).string(v);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseCreatePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.chatGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }
          message.options.push(reader.string());
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      chatGuid: isSet7(object.chatGuid) ? globalThis.String(object.chatGuid) : isSet7(object.chat_guid) ? globalThis.String(object.chat_guid) : "",
      title: isSet7(object.title) ? globalThis.String(object.title) : "",
      options: globalThis.Array.isArray(object?.options) ? object.options.map((e) => globalThis.String(e)) : [],
      clientMessageId: isSet7(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet7(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.chatGuid !== "") {
      obj.chatGuid = message.chatGuid;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.options?.length) {
      obj.options = message.options;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return CreatePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseCreatePollRequest();
    message.chatGuid = object.chatGuid ?? "";
    message.title = object.title ?? "";
    message.options = object.options?.map((e) => e) || [];
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBasePollResponse() {
  return { poll: void 0 };
}
var PollResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.poll !== void 0) {
      PollInfo.encode(message.poll, writer.uint32(10).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBasePollResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.poll = PollInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return { poll: isSet7(object.poll) ? PollInfo.fromJSON(object.poll) : void 0 };
  },
  toJSON(message) {
    const obj = {};
    if (message.poll !== void 0) {
      obj.poll = PollInfo.toJSON(message.poll);
    }
    return obj;
  },
  create(base) {
    return PollResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBasePollResponse();
    message.poll = object.poll !== void 0 && object.poll !== null ? PollInfo.fromPartial(object.poll) : void 0;
    return message;
  }
};
function createBaseVotePollRequest() {
  return { pollMessageGuid: "", optionIdentifier: "", clientMessageId: void 0 };
}
var VotePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.optionIdentifier !== "") {
      writer.uint32(18).string(message.optionIdentifier);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseVotePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionIdentifier = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet7(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet7(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      optionIdentifier: isSet7(object.optionIdentifier) ? globalThis.String(object.optionIdentifier) : isSet7(object.option_identifier) ? globalThis.String(object.option_identifier) : "",
      clientMessageId: isSet7(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet7(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.optionIdentifier !== "") {
      obj.optionIdentifier = message.optionIdentifier;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return VotePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseVotePollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.optionIdentifier = object.optionIdentifier ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseUnvotePollRequest() {
  return { pollMessageGuid: "", clientMessageId: void 0 };
}
var UnvotePollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseUnvotePollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet7(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet7(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      clientMessageId: isSet7(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet7(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return UnvotePollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseUnvotePollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseAddPollOptionRequest() {
  return { pollMessageGuid: "", optionText: "", clientMessageId: void 0 };
}
var AddPollOptionRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    if (message.optionText !== "") {
      writer.uint32(18).string(message.optionText);
    }
    if (message.clientMessageId !== void 0) {
      writer.uint32(802).string(message.clientMessageId);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseAddPollOptionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }
          message.optionText = reader.string();
          continue;
        }
        case 100: {
          if (tag !== 802) {
            break;
          }
          message.clientMessageId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet7(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet7(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : "",
      optionText: isSet7(object.optionText) ? globalThis.String(object.optionText) : isSet7(object.option_text) ? globalThis.String(object.option_text) : "",
      clientMessageId: isSet7(object.clientMessageId) ? globalThis.String(object.clientMessageId) : isSet7(object.client_message_id) ? globalThis.String(object.client_message_id) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    if (message.optionText !== "") {
      obj.optionText = message.optionText;
    }
    if (message.clientMessageId !== void 0) {
      obj.clientMessageId = message.clientMessageId;
    }
    return obj;
  },
  create(base) {
    return AddPollOptionRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseAddPollOptionRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    message.optionText = object.optionText ?? "";
    message.clientMessageId = object.clientMessageId ?? void 0;
    return message;
  }
};
function createBaseGetPollRequest() {
  return { pollMessageGuid: "" };
}
var GetPollRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== "") {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseGetPollRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet7(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet7(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : ""
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== "") {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    return obj;
  },
  create(base) {
    return GetPollRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseGetPollRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? "";
    return message;
  }
};
function createBaseSubscribePollEventsRequest() {
  return { pollMessageGuid: void 0 };
}
var SubscribePollEventsRequest = {
  encode(message, writer = new BinaryWriter()) {
    if (message.pollMessageGuid !== void 0) {
      writer.uint32(10).string(message.pollMessageGuid);
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribePollEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }
          message.pollMessageGuid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      pollMessageGuid: isSet7(object.pollMessageGuid) ? globalThis.String(object.pollMessageGuid) : isSet7(object.poll_message_guid) ? globalThis.String(object.poll_message_guid) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.pollMessageGuid !== void 0) {
      obj.pollMessageGuid = message.pollMessageGuid;
    }
    return obj;
  },
  create(base) {
    return SubscribePollEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribePollEventsRequest();
    message.pollMessageGuid = object.pollMessageGuid ?? void 0;
    return message;
  }
};
function createBaseSubscribePollEventsResponse() {
  return { sequence: void 0, pollChanged: void 0, heartbeat: void 0 };
}
var SubscribePollEventsResponse = {
  encode(message, writer = new BinaryWriter()) {
    if (message.sequence !== void 0) {
      writer.uint32(8).uint64(message.sequence);
    }
    if (message.pollChanged !== void 0) {
      PollChangeEvent.encode(message.pollChanged, writer.uint32(82).fork()).join();
    }
    if (message.heartbeat !== void 0) {
      Heartbeat.encode(message.heartbeat, writer.uint32(794).fork()).join();
    }
    return writer;
  },
  decode(input, length) {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === void 0 ? reader.len : reader.pos + length;
    const message = createBaseSubscribePollEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }
          message.sequence = longToNumber4(reader.uint64());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }
          message.pollChanged = PollChangeEvent.decode(reader, reader.uint32());
          continue;
        }
        case 99: {
          if (tag !== 794) {
            break;
          }
          message.heartbeat = Heartbeat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },
  fromJSON(object) {
    return {
      sequence: isSet7(object.sequence) ? globalThis.Number(object.sequence) : void 0,
      pollChanged: isSet7(object.pollChanged) ? PollChangeEvent.fromJSON(object.pollChanged) : isSet7(object.poll_changed) ? PollChangeEvent.fromJSON(object.poll_changed) : void 0,
      heartbeat: isSet7(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : void 0
    };
  },
  toJSON(message) {
    const obj = {};
    if (message.sequence !== void 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.pollChanged !== void 0) {
      obj.pollChanged = PollChangeEvent.toJSON(message.pollChanged);
    }
    if (message.heartbeat !== void 0) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    return obj;
  },
  create(base) {
    return SubscribePollEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object) {
    const message = createBaseSubscribePollEventsResponse();
    message.sequence = object.sequence ?? void 0;
    message.pollChanged = object.pollChanged !== void 0 && object.pollChanged !== null ? PollChangeEvent.fromPartial(object.pollChanged) : void 0;
    message.heartbeat = object.heartbeat !== void 0 && object.heartbeat !== null ? Heartbeat.fromPartial(object.heartbeat) : void 0;
    return message;
  }
};
var PollServiceDefinition = {
  name: "PollService",
  fullName: "photon.imessage.v1.PollService",
  methods: {
    /** Writes */
    createPoll: {
      name: "CreatePoll",
      requestType: CreatePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [new Uint8Array([14, 58, 1, 42, 34, 9, 47, 118, 49, 47, 112, 111, 108, 108, 115])]
        }
      }
    },
    votePoll: {
      name: "VotePoll",
      requestType: VotePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              19,
              58,
              1,
              42,
              34,
              14,
              47,
              118,
              49,
              47,
              112,
              111,
              108,
              108,
              115,
              58,
              118,
              111,
              116,
              101
            ])
          ]
        }
      }
    },
    unvotePoll: {
      name: "UnvotePoll",
      requestType: UnvotePollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              21,
              58,
              1,
              42,
              34,
              16,
              47,
              118,
              49,
              47,
              112,
              111,
              108,
              108,
              115,
              58,
              117,
              110,
              118,
              111,
              116,
              101
            ])
          ]
        }
      }
    },
    addPollOption: {
      name: "AddPollOption",
      requestType: AddPollOptionRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              24,
              58,
              1,
              42,
              34,
              19,
              47,
              118,
              49,
              47,
              112,
              111,
              108,
              108,
              115,
              58,
              97,
              100,
              100,
              79,
              112,
              116,
              105,
              111,
              110
            ])
          ]
        }
      }
    },
    /** Reads (poll message GUIDs are path-safe) */
    getPoll: {
      name: "GetPoll",
      requestType: GetPollRequest,
      requestStream: false,
      responseType: PollResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            new Uint8Array([
              31,
              18,
              29,
              47,
              118,
              49,
              47,
              112,
              111,
              108,
              108,
              115,
              47,
              123,
              112,
              111,
              108,
              108,
              95,
              109,
              101,
              115,
              115,
              97,
              103,
              101,
              95,
              103,
              117,
              105,
              100,
              125
            ])
          ]
        }
      }
    },
    /** Streams */
    subscribePollEvents: {
      name: "SubscribePollEvents",
      requestType: SubscribePollEventsRequest,
      requestStream: false,
      responseType: SubscribePollEventsResponse,
      responseStream: true,
      options: {}
    }
  }
};
function longToNumber4(int64) {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}
function isSet7(value) {
  return value !== null && value !== void 0;
}

export { AddressServiceDefinition, AttachmentServiceDefinition, ChatServiceDefinition, GroupServiceDefinition, LocationServiceDefinition, MessageServiceDefinition, PollServiceDefinition };
//# sourceMappingURL=chunk-UXIJEFIK.js.map
//# sourceMappingURL=chunk-UXIJEFIK.js.map