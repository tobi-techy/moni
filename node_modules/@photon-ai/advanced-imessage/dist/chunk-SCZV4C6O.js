import { ConnectionError, TypedEventStream } from './chunk-KA2VVWQX.js';

// ../grpc/src/index.ts
var PEER_HINT = '`createGrpcClient` needs the optional peer dependencies of "@photon-ai/advanced-imessage": nice-grpc, nice-grpc-common, and @grpc/grpc-js.\nInstall them:  bun add nice-grpc nice-grpc-common @grpc/grpc-js\nThe gRPC transport is legacy; new code should use `createHttpClient`, which needs no extra installs.';
var GRPC_STATUS_UNAVAILABLE = 14;
function missingPeersError(cause) {
  return new ConnectionError(PEER_HINT, {
    cause: cause instanceof Error ? cause : void 0,
    code: "internalError",
    context: {},
    grpcCode: GRPC_STATUS_UNAVAILABLE,
    retryable: false
  });
}
var modulePromise;
function loadV1() {
  modulePromise ??= import('./client-NRHONKWO.js').catch((cause) => {
    modulePromise = void 0;
    throw missingPeersError(cause);
  });
  return modulePromise;
}
function assertPeersResolvable() {
  const meta = import.meta;
  if (typeof meta.resolve !== "function") {
    return;
  }
  try {
    for (const peer of ["nice-grpc", "nice-grpc-common", "@grpc/grpc-js"]) {
      meta.resolve(peer);
    }
  } catch (cause) {
    throw missingPeersError(cause);
  }
}
var NO_STREAMS = /* @__PURE__ */ new Set();
var STREAM_METHODS = {
  attachments: /* @__PURE__ */ new Set(["downloadStream"]),
  chats: /* @__PURE__ */ new Set(["subscribeEvents"]),
  events: /* @__PURE__ */ new Set(["catchUp"]),
  groups: /* @__PURE__ */ new Set(["subscribeEvents"]),
  locations: /* @__PURE__ */ new Set(["watch"]),
  messages: /* @__PURE__ */ new Set(["subscribeEvents"]),
  polls: /* @__PURE__ */ new Set(["subscribeEvents"])
};
function invokeMember(target, key, prop, args) {
  const member = Reflect.get(target, prop);
  if (typeof member !== "function") {
    throw new TypeError(`${key}.${prop} is not a method`);
  }
  return member.apply(target, args);
}
function lazyStream(client, key, prop, args) {
  let inner;
  let closed = false;
  async function* run() {
    const resource = (await client)[key];
    if (closed) {
      return;
    }
    inner = invokeMember(
      resource,
      key,
      prop,
      args
    );
    yield* inner;
  }
  return new TypedEventStream(run(), async () => {
    closed = true;
    if (inner) {
      await inner.close();
    }
  });
}
function lazyResource(client, key, streamMethods) {
  const methods = /* @__PURE__ */ new Map();
  return new Proxy({}, {
    get(_target, prop) {
      if (typeof prop !== "string" || prop === "then") {
        return void 0;
      }
      let method = methods.get(prop);
      if (!method) {
        method = streamMethods.has(prop) ? (...args) => lazyStream(client, key, prop, args) : (...args) => client.then((c) => invokeMember(c[key], key, prop, args));
        methods.set(prop, method);
      }
      return method;
    }
  });
}
function createGrpcClient(options) {
  assertPeersResolvable();
  const client = loadV1().then((m) => m.createClient(options));
  client.catch(() => void 0);
  async function close() {
    const c = await client.catch(() => void 0);
    if (c) {
      await c.close();
    }
  }
  return {
    addresses: lazyResource(client, "addresses", NO_STREAMS),
    attachments: lazyResource(
      client,
      "attachments",
      STREAM_METHODS.attachments ?? NO_STREAMS
    ),
    chats: lazyResource(client, "chats", STREAM_METHODS.chats ?? NO_STREAMS),
    events: lazyResource(client, "events", STREAM_METHODS.events ?? NO_STREAMS),
    groups: lazyResource(client, "groups", STREAM_METHODS.groups ?? NO_STREAMS),
    locations: lazyResource(
      client,
      "locations",
      STREAM_METHODS.locations ?? NO_STREAMS
    ),
    messages: lazyResource(
      client,
      "messages",
      STREAM_METHODS.messages ?? NO_STREAMS
    ),
    polls: lazyResource(client, "polls", STREAM_METHODS.polls ?? NO_STREAMS),
    close,
    async [Symbol.asyncDispose]() {
      await close();
    }
  };
}
var createClient = createGrpcClient;

export { createClient, createGrpcClient };
//# sourceMappingURL=chunk-SCZV4C6O.js.map
//# sourceMappingURL=chunk-SCZV4C6O.js.map