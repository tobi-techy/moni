import { Address } from '@1inch/limit-order-sdk';
export declare class TargetOnlyInteraction {
    readonly target: Address;
    readonly data = "0x";
    constructor(target: Address);
    encode(): string;
}
