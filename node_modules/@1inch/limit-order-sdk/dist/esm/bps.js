function _array_like_to_array(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for(var i = 0, arr2 = new Array(len); i < len; i++)arr2[i] = arr[i];
    return arr2;
}
function _array_with_holes(arr) {
    if (Array.isArray(arr)) return arr;
}
function _class_call_check(instance, Constructor) {
    if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
    }
}
function _defineProperties(target, props) {
    for(var i = 0; i < props.length; i++){
        var descriptor = props[i];
        descriptor.enumerable = descriptor.enumerable || false;
        descriptor.configurable = true;
        if ("value" in descriptor) descriptor.writable = true;
        Object.defineProperty(target, descriptor.key, descriptor);
    }
}
function _create_class(Constructor, protoProps, staticProps) {
    if (protoProps) _defineProperties(Constructor.prototype, protoProps);
    if (staticProps) _defineProperties(Constructor, staticProps);
    return Constructor;
}
function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else {
        obj[key] = value;
    }
    return obj;
}
function _iterable_to_array_limit(arr, i) {
    var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"];
    if (_i == null) return;
    var _arr = [];
    var _n = true;
    var _d = false;
    var _s, _e;
    try {
        for(_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true){
            _arr.push(_s.value);
            if (i && _arr.length === i) break;
        }
    } catch (err) {
        _d = true;
        _e = err;
    } finally{
        try {
            if (!_n && _i["return"] != null) _i["return"]();
        } finally{
            if (_d) throw _e;
        }
    }
    return _arr;
}
function _non_iterable_rest() {
    throw new TypeError("Invalid attempt to destructure non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _sliced_to_array(arr, i) {
    return _array_with_holes(arr) || _iterable_to_array_limit(arr, i) || _unsupported_iterable_to_array(arr, i) || _non_iterable_rest();
}
function _unsupported_iterable_to_array(o, minLen) {
    if (!o) return;
    if (typeof o === "string") return _array_like_to_array(o, minLen);
    var n = Object.prototype.toString.call(o).slice(8, -1);
    if (n === "Object" && o.constructor) n = o.constructor.name;
    if (n === "Map" || n === "Set") return Array.from(n);
    if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _array_like_to_array(o, minLen);
}
import assert from 'assert';
/**
 * Basis point in range [0, 100]%
 *
 * 1bps = 0.01%
 */ export var Bps = /*#__PURE__*/ function() {
    "use strict";
    function Bps(value) {
        _class_call_check(this, Bps);
        _define_property(this, "value", void 0);
        this.value = value;
        assert(value >= 0 && value <= 10000, "invalid bps ".concat(value));
    }
    _create_class(Bps, [
        {
            key: "equal",
            value: function equal(other) {
                return this.value === other.value;
            }
        },
        {
            key: "isZero",
            value: function isZero() {
                return this.value === 0n;
            }
        },
        {
            key: "toPercent",
            value: /**
     * @param base what represents 100%
     */ function toPercent() {
                var base = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1n;
                return Number(this.value * base) / 100;
            }
        },
        {
            key: "toFraction",
            value: /**
     * @param base what represents 100%
     */ function toFraction() {
                var base = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1n;
                return Number(this.value * base) / 10000;
            }
        },
        {
            key: "toString",
            value: function toString() {
                return this.value.toString();
            }
        }
    ], [
        {
            key: "fromPercent",
            value: /**
     * Create BPS from percent value.
     * If `value` has precision more than 1bps (with accounting to `base`), it will be rounded down
     * @param val
     * @param base what represents 100%
     */ function fromPercent(val) {
                var base = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1n;
                return Bps.fromSharePercent(val, base);
            }
        },
        {
            key: "fromSharePercent",
            value: /**
     * Create BPS from a percent value (0–100) using decimal-safe parsing.
     * E.g. 84.45% → 8445 bps
     */ function fromSharePercent(val) {
                var base = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1n;
                var normalized = typeof val === 'number' ? val.toString() : val.trim();
                assert(!normalized.startsWith('-'), "invalid bps ".concat(normalized));
                var _normalized_split = _sliced_to_array(normalized.split('.'), 2), tmp = _normalized_split[0], wholePart = tmp === void 0 ? '0' : tmp, tmp1 = _normalized_split[1], fracPart = tmp1 === void 0 ? '' : tmp1;
                var fracBps = (fracPart + '00').slice(0, 2);
                var bps = BigInt(wholePart || '0') * 100n + BigInt(fracBps || '0');
                return new Bps(bps / base);
            }
        },
        {
            key: "fromFraction",
            value: /**
     * Create BPS from fraction value.
     * If `value` has precision more than 1bps (with accounting to `base`), it will be rounded down
     * @param val
     * @param base what represents 100%
     */ function fromFraction(val) {
                var base = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 1n;
                return new Bps(BigInt(10000 * val) / base);
            }
        }
    ]);
    return Bps;
}();
_define_property(Bps, "ZERO", new Bps(0n));
