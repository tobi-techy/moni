function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
    try {
        var info = gen[key](arg);
        var value = info.value;
    } catch (error) {
        reject(error);
        return;
    }
    if (info.done) {
        resolve(value);
    } else {
        Promise.resolve(value).then(_next, _throw);
    }
}
function _async_to_generator(fn) {
    return function() {
        var self = this, args = arguments;
        return new Promise(function(resolve, reject) {
            var gen = fn.apply(self, args);
            function _next(value) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
            }
            function _throw(err) {
                asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
            }
            _next(undefined);
        });
    };
}
function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else {
        obj[key] = value;
    }
    return obj;
}
function _object_spread(target) {
    for(var i = 1; i < arguments.length; i++){
        var source = arguments[i] != null ? arguments[i] : {};
        var ownKeys = Object.keys(source);
        if (typeof Object.getOwnPropertySymbols === "function") {
            ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function(sym) {
                return Object.getOwnPropertyDescriptor(source, sym).enumerable;
            }));
        }
        ownKeys.forEach(function(key) {
            _define_property(target, key, source[key]);
        });
    }
    return target;
}
function ownKeys(object, enumerableOnly) {
    var keys = Object.keys(object);
    if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) {
            symbols = symbols.filter(function(sym) {
                return Object.getOwnPropertyDescriptor(object, sym).enumerable;
            });
        }
        keys.push.apply(keys, symbols);
    }
    return keys;
}
function _object_spread_props(target, source) {
    source = source != null ? source : {};
    if (Object.getOwnPropertyDescriptors) {
        Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
        ownKeys(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
        });
    }
    return target;
}
function _ts_generator(thisArg, body) {
    var f, y, t, _ = {
        label: 0,
        sent: function() {
            if (t[0] & 1) throw t[1];
            return t[1];
        },
        trys: [],
        ops: []
    }, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype), d = Object.defineProperty;
    return d(g, "next", {
        value: verb(0)
    }), d(g, "throw", {
        value: verb(1)
    }), d(g, "return", {
        value: verb(2)
    }), typeof Symbol === "function" && d(g, Symbol.iterator, {
        value: function() {
            return this;
        }
    }), g;
    function verb(n) {
        return function(v) {
            return step([
                n,
                v
            ]);
        };
    }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while(g && (g = 0, op[0] && (_ = 0)), _)try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [
                op[0] & 2,
                t.value
            ];
            switch(op[0]){
                case 0:
                case 1:
                    t = op;
                    break;
                case 4:
                    _.label++;
                    return {
                        value: op[1],
                        done: false
                    };
                case 5:
                    _.label++;
                    y = op[1];
                    op = [
                        0
                    ];
                    continue;
                case 7:
                    op = _.ops.pop();
                    _.trys.pop();
                    continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                        _ = 0;
                        continue;
                    }
                    if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                        _.label = op[1];
                        break;
                    }
                    if (op[0] === 6 && _.label < t[1]) {
                        _.label = t[1];
                        t = op;
                        break;
                    }
                    if (t && _.label < t[2]) {
                        _.label = t[2];
                        _.ops.push(op);
                        break;
                    }
                    if (t[2]) _.ops.pop();
                    _.trys.pop();
                    continue;
            }
            op = body.call(thisArg, _);
        } catch (e) {
            op = [
                6,
                e
            ];
            y = 0;
        } finally{
            f = t = 0;
        }
        if (op[0] & 5) throw op[1];
        return {
            value: op[0] ? op[1] : void 0,
            done: true
        };
    }
}
import { Sdk } from './sdk.js';
import { Address } from '../address.js';
import { Bps } from '../bps.js';
import { FeeTakerExt, MakerTraits } from '../limit-order/index.js';
describe('Sdk.createOrder', function() {
    var mockHttpConnector;
    var sdk;
    var maker = new Address('0x1234567890123456789012345678901234567890');
    var makerAsset = new Address('0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48');
    var takerAsset = new Address('0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2');
    // /fee-info (USDC/WETH) with portal fee headers (integrator-normal-pair-fee 0.07, rev-share 15, provider my-dapp).
    var protocolFeeReceiver = '0x90cbe4bdd538d6e9b379bff5fe72c3d67a521de5';
    var integratorFeeReceiver = '0x2222222222222222222222222222222222222222';
    var extensionAddress = '0xc0dfdb9e7a392c3dbbe7c6fbe8fbc1789c9fe05e';
    var source = 'aba10994';
    // bare hex from API (provider track code)
    var orderInfo = {
        makerAsset: makerAsset,
        takerAsset: takerAsset,
        makingAmount: 1000000n,
        takingAmount: 2000000000000000000n,
        maker: maker
    };
    var baseFeeInfo = {
        whitelist: {
            '0x7246999fd1bab15b4ac7d1a23c3abeed63c51b86': '0x9c4dffb4f7e8217a8ac0555d67e125f8769284ba'
        },
        feeBps: 30,
        whitelistDiscountPercent: 0,
        protocolFeeReceiver: protocolFeeReceiver,
        extensionAddress: extensionAddress,
        source: 'abcd1234'
    };
    var feeInfoWithIntegrator = _object_spread_props(_object_spread({}, baseFeeInfo), {
        integratorFeeBps: 7,
        integratorFeeReceiver: integratorFeeReceiver,
        integratorFeeSharePercent: 85,
        source: source
    });
    beforeEach(function() {
        mockHttpConnector = {
            get: jest.fn(),
            post: jest.fn()
        };
        sdk = new Sdk({
            networkId: 1,
            authKey: 'test-auth-key',
            httpConnector: mockHttpConnector,
            baseUrl: 'https://api.test.com'
        });
    });
    it('embeds integrator fee from fee-info', function() {
        return _async_to_generator(function() {
            var order;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        mockHttpConnector.get.mockResolvedValueOnce(feeInfoWithIntegrator);
                        return [
                            4,
                            sdk.createOrder(orderInfo, MakerTraits.default())
                        ];
                    case 1:
                        order = _state.sent();
                        expect(order.feeExtension.fees.integrator.fee.value).toBe(7n);
                        expect(order.feeExtension.fees.integrator.share.value).toBe(8500n);
                        expect(order.feeExtension.fees.integrator.integrator.toString()).toBe(integratorFeeReceiver);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('sets source track code from fee-info', function() {
        return _async_to_generator(function() {
            var order;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        mockHttpConnector.get.mockResolvedValueOnce(feeInfoWithIntegrator);
                        return [
                            4,
                            sdk.createOrder(orderInfo)
                        ];
                    case 1:
                        order = _state.sent();
                        // API returns bare hex; setSource keccak-hashes non-0x values
                        expect(order.getTrackCode()).toBe('0xb5e948e6');
                        expect(order.build().salt).toBe(order.salt.toString());
                        order.setSource('0xdeadbeef');
                        expect(order.getTrackCode()).toBe('0xdeadbeef');
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('does not overwrite track code when salt is provided', function() {
        return _async_to_generator(function() {
            var seeded, order;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        mockHttpConnector.get.mockResolvedValue(feeInfoWithIntegrator);
                        return [
                            4,
                            sdk.createOrder(orderInfo)
                        ];
                    case 1:
                        seeded = _state.sent();
                        seeded.setSource('0xdeadbeef');
                        return [
                            4,
                            sdk.createOrder(_object_spread_props(_object_spread({}, orderInfo), {
                                salt: seeded.salt
                            }))
                        ];
                    case 2:
                        order = _state.sent();
                        expect(order.getTrackCode()).toBe('0xdeadbeef');
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('uses ZERO integrator fee when fee-info has no integrator fields', function() {
        return _async_to_generator(function() {
            var order;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        mockHttpConnector.get.mockResolvedValueOnce(baseFeeInfo);
                        return [
                            4,
                            sdk.createOrder(orderInfo)
                        ];
                    case 1:
                        order = _state.sent();
                        expect(order.feeExtension.fees.integrator).toEqual(FeeTakerExt.IntegratorFee.ZERO);
                        return [
                            2
                        ];
                }
            });
        })();
    });
    it('prefers explicit integratorFee override', function() {
        return _async_to_generator(function() {
            var customIntegratorFee, order;
            return _ts_generator(this, function(_state) {
                switch(_state.label){
                    case 0:
                        mockHttpConnector.get.mockResolvedValueOnce(feeInfoWithIntegrator);
                        customIntegratorFee = new FeeTakerExt.IntegratorFee(new Address(integratorFeeReceiver), new Address(protocolFeeReceiver), new Bps(100n), new Bps(8000n));
                        return [
                            4,
                            sdk.createOrder(orderInfo, MakerTraits.default(), {
                                integratorFee: customIntegratorFee
                            })
                        ];
                    case 1:
                        order = _state.sent();
                        expect(order.feeExtension.fees.integrator.fee.value).toBe(100n);
                        expect(order.feeExtension.fees.integrator.share.value).toBe(8000n);
                        return [
                            2
                        ];
                }
            });
        })();
    });
});
